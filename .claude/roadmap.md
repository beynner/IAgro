# Roadmap & Estado

Mapa vivo do projeto. Atualizar a cada marco.

---

## Módulos prontos para produção

Funcionam ponta-a-ponta. Tratáveis como concluídos após validação de `DEBUG=False` em ambiente real.

| Módulo | Estado | Notas |
|---|---|---|
| **Entrada (TOP 11)** | ✅ Concluído | Recebimento, conferência, pesagem, lote auto-gerado. Coberto em `test_views_entrada.py` |
| **Classificação (TOP 26)** | ✅ Concluído | Triagem por qualidade, descartes via `AD_QTDAVARIA`. **Sem cobertura dedicada** (pendência) |
| **Comercial (TOP 13)** | ✅ Concluído | Faturamento de vales, precificação, financeiro em TGFFIN, simulações em SQLite. Coberto em `test_views_comercial.py` |
| **Venda — núcleo (TOP 34)** | ✅ Concluído | Criar/editar/excluir cabeçalho; adicionar/editar/remover item. UX para não-tech (resumo, dedup, empty state, CODNAT dropdown). 90 testes |
| **Venda — faturamento (34→35/37)** | ✅ Concluído | Lock pessimista, validação de itens com lote, NUMNOTA por empresa, `CODNAT_POR_TOP`. **Não dispara emissão real de NFe** (decisão consciente — Sankhya cuida) |
| **Rastreio — atribuição/desvinculação** | ✅ Concluído | View `ANDRE_IAGRO_SALDO_LOTE` (5 pernas), filtros cruzados opt-in, lock pessimista, `RastreioAudit` em SQLite |
| **Rastreio — UI/UX** | ✅ Concluído | Paleta sóbria alinhada ao `home.css` (verde Agromil + marrom + accent) |
| **Robustez geral** | ✅ Concluído | `humanizar_erro_oracle()`, atomicidade transacional, `@ensure_csrf_cookie`, `IAgro.confirmarAcao()` |
| **Helpers JS reutilizáveis** | ✅ Concluído | `confirmarAcao`, `cachedFetch`, `postJSON`, `showToast`, `debounce`, `getCookie` |
| **Healthcheck profundo** | ✅ Concluído | `/sankhya/health/?deep=1` valida ping Oracle, view do WMS, contagem de TOP 34 |
| **Venda — UX dos filtros** (sessão Abr/2026) | ✅ Concluído | Card lateral `#cardFiltros` com todos os campos + chips de filtros ativos + typeahead de Empresa + persistência em `localStorage` (chave `iagro:venda:filtros:v1`) + refino cosmético no input de Lote (placeholder/title) |
| **Importação de pedidos por e-mail v1** (sessão Mai/2026) | ✅ Código concluído · ⚠ Setup quase completo | Worker IMAP (Titan), parser LLM local (Ollama + **Qwen 2.5 14B** — subido de 7B após confirmar hardware do servidor), 8 endpoints, tela de revisão e promoção para TGFCAB TOP 34 reusando APIs da Venda. **56 testes novos (174 → 230).** |
| **Importação por e-mail v2 — multi-pedido + matching híbrido + aprendizado** (sessão Mai/2026) | ✅ Código concluído · ✅ End-to-end testado | Refatoração B+C: split de PDF em N pedidos via `split_pedidos()` (1 PDF Assaí com 3 lojas → 3 pré-pedidos com SUB_ID 1/2/3), prompt LLM SEM contexto de IDs (anti-alucinação) com few-shot reforçado, novo `services/matching.py` (rapidfuzz + token_sort_ratio pra parceiros / WRatio pra produtos) consultando alias antes do fuzzy. **2 tabelas novas** (`AD_PRODUTO_ALIAS`, `AD_PARCEIRO_ALIAS`) gravadas no clique de Confirmar — não é ML, é dicionário determinístico auditável. **2 endpoints novos** (`Restaurar tudo`, `Restaurar item`) que reusam o JSON crú do LLM já salvo, sem chamar Ollama de novo (~1s vs ~3 min do Reparser). **Tela**: coluna "Nosso produto" via JOIN TGFPRO, hint "Nosso cliente" via JOIN TGFPAR, tfoot com Σ qtd e Σ valor reativos, typeahead de produto por linha, botão `↺` por linha. **Resultados em teste**: 1 PDF Assaí → 3 pré-pedidos × 14/18/15 itens (47 totais), 100% qtd/preço corretos, ~88% matching CODPROD automático. Performance: ~3 min/pedido após split (vs >15 min antes). |
| **Importação por e-mail v3 — Fase 1 (split físico PDF) + Fase 2 (layouts plug) + Fase 3 (paste manual texto livre)** (sessão Mai/2026, 2026-05-07) | ✅ Código concluído · ✅ Smoke testado · ⚠ Tests legados não refatorados | **Fase 1 — Split físico do PDF**: refator de `split_pedidos()` por página (cobre Caso 1 — pedido transbordando + Caso 2 — header repetido em continuação) + `split_pdf_fisico()` via pypdf cria `<MSGID>_sub{N}.pdf` por SUB_ID. PDF original preservado. Comando `resplit_pdfs_email` faz backfill idempotente do legado. **Fase 2 — Layouts plugáveis**: `_HEADERS_POR_LAYOUT` (worker) + `PROMPTS_POR_LAYOUT` (LLM) — adicionar layout novo = 2 entradas em dicts. Layouts atuais: `CONSINCO_RELPED` (Assaí/SENDAS, prompt com avisos específicos) e `GENERICO` (fallback neutro). **Fase 3 — Paste manual de texto livre**: botão `📋 Importar texto` no header da fila → modal com textarea (≥30 chars) + select origem (TEXTO_LIVRE / WHATSAPP_API) → cria registro com PDF_PATH=NULL e ORIGEM. Frontend mostra `<pre>` em vez de iframe quando origem != IMAP. Worker pula extração de PDF (texto já em PDF_TEXTO). |
| **Importação por e-mail v4 — Vinculação por código do cliente (`cod_cliente`)** (sessão Mai/2026, 2026-05-07) | ✅ Código concluído · ✅ End-to-end testado · ⚠ Tests legados não refatorados | Tabela nova `AD_CLIENTE_PRODUTO_COD` (chave `(CODPARC, COD_CLIENTE)`) + coluna `COD_CLIENTE` em `AD_PEDIDO_EMAIL_ITEM`. Prompt LLM Consinco extrai `cod_cliente` ("Cod Forn") por item. Matching ganhou Etapa 0: lookup direto na tabela com confiança 100. Após operador confirmar 1 pedido Assaí, **TODOS os próximos pedidos da Assaí casam itens automaticamente** sem fuzzy/alias. Frontend: coluna nova "Cód cliente" entre Texto do PDF e CODPROD. **Schema-resilient**: `_existe_coluna()` cache permite código rodar antes/depois das migrations. |
| **Importação por e-mail v5 — Etapa 0 regex parser + telemetria visual** (sessão Mai/2026, 2026-05-07) | ✅ Código concluído · ⚠ Em testes em produção — **freeze do módulo** | **Fase A — infra plugável**: `services/pdf_parsers/{__init__.py, consinco.py}` com registry `PARSERS_POR_LAYOUT` + dispatcher `tentar_parseamento()`. Worker tenta parser específico do layout antes do LLM; falha cai em fallback transparente. **Fase B — parser Consinco real**: regex sobre `pdf_texto` com âncora `<VOL> <VOL> <EMB=1> <QTDE> <VLR_UNIT> <VLR_ITEM>`. Validações cruzadas: `Total de itens declarado == len(itens)` + `\|Σ valor − Total geral PDF\| ≤ R$ 0,10`. Conservador: divergência → None → fallback LLM. ~50ms vs ~3min do LLM. Telemetria via `LLM_MODELO='regex_consinco_v1'`. **Fase C — UI de conferência**: badge `⚡ Regex` / `🧠 LLM` no header + 2ª linha `<tfoot>` mostrando totais declarados no PDF + chip `✓ Bate com PDF` / `⚠ Diverge`. `extrair_totais_pdf()` reusado em `api_email_obter` — linha de conferência aparece também em registros LLM. `LLM_RESPOSTA` mantém shape — Restaurar tudo/item funcionam nos 2 caminhos. **Aguardando 1 semana de produção antes de novas iterações no módulo email.** |
| **UX padrão pra módulos novos** (Mai/2026) | ✅ Documentado em `conventions.md` | Typeaheads navegáveis (↓/↑/Tab/Enter/Esc), select-all-on-focus, defaults sensíveis (KG/CODEMP=10/CODNAT=10010100), formato `cod — NOME` em campos pré-populados. **Aplicado apenas no módulo email** (não retrofitado nos antigos sem pedido explícito). |
| **Defesa anti-duplicação em camadas** (Mai/2026) | ✅ Concluído | Worker faz DELETE defensivo antes de cada INSERT (mesmo se reparser falhar). `api_email_reparser` usa batch DELETE pelo RECEBIDO_ID em vez de loop falível. Bug real corrigido (3 pré-pedidos, 1 duplicou — 28 itens em vez de 14). |
| **Etiquetas SafeTrace/IAgro (Rastreio)** (sessão Mai/2026, 2026-05-14) | ✅ Concluído · ⚠ Em testes em produção | **Impressão de etiquetas de rastreabilidade 100×50mm landscape** pra Zebra ZD220. 1 etiqueta = 1 caixa do pedido. Cálculo determinístico `math.ceil(qtdneg / qtdfixada)`. **Backend Cat B**: `atribuir_lote_item_pedido` ganhou parâmetro opcional `qtdfixada` (digitado pelo operador no modal — sem busca automática); UPDATE/INSERT da TGFITE gravam o valor. `desvincular_lote_item_pedido` limpa CODAGREGACAO+QTDFIXADA juntos. **Backend Cat A**: `consultar_dados_etiqueta_pedido(nunota, codprod=None)` lê TGFCAB+TGFITE+TGFPAR+TGFPRO+TSIEMP em 1 query com `_existe_coluna()` pra colunas opcionais (ENDERECO/CEP/LOGRADOURO). `calcular_qtd_etiquetas(qtd_kg, peso_caixa_kg)` arredonda pra cima. Endpoint `GET /sankhya/rastreio/api/etiqueta-pdf/?nunota=X[&codprod=Y]`. **Serviço novo** `services/etiqueta_lote.py` com reportlab (PDF) + qrcode (QR code com URL pública via `URL_RASTREIO_PUBLICA` no `.env`) + reportlab.graphics.barcode (EAN13 lido de `TGFPRO.REFERENCIA`). **Frontend**: botão 🖨 (`.btn-etiqueta` em verde Agromil, sempre visível) no header de pedido e em cada produto-linha — aparece só onde `qtd_atribuida > 0`. Modal de transferência ganhou campo **"Peso da caixa (kg) *"** obrigatório (REPOLHO=16, TOMATE=20, PIMENTÃO=10). Click no botão abre PDF em aba nova. **17 tests novos** em `test_etiqueta_lote.py` (helper, render, view, regressão B1/B2). **Pendente**: definir URL pública real e backfill de QTDFIXADA pra pedidos antigos que precisarão ser re-vinculados. |
| **Login + tela de entrada (UX polish)** (sessão Mai/2026, 2026-05-14) | ✅ Concluído | Imagem de fundo `login-bg.png` com overlay verde Agromil (gradiente translúcido 70%/65%). Botão "Entrar" agora alinhado aos inputs (`width: 100%`), com spinner CSS + disable no submit pra evitar double-click. Cache `?v=5` no `home.css`. |
| **Sidebar retraída por padrão + abastecimento externo triangular** (sessão Mai/2026, 2026-05-14) | ✅ Concluído | **Sidebar**: primeira visita = retraída (operador que expandir explicitamente grava `'0'` no localStorage e mantém preferência). Inline script no `base.html` + `setupSidebar()` em `iagro_helpers.js` aplicam mesma regra. **Abastecimento externo (Combustível)**: campo "Valor Total" do item virou input editável (`.cb-input-reqext-tot`); lógica triangular qtd ↔ vlrunit ↔ vlrtot — operador edita qualquer um dos 3 e o sistema calcula o outro automaticamente: editou vlrtot → recalcula vlrunit (qtd nunca é alterada, vem da bomba). Validação no salvar exige os 3 campos preenchidos. |
| **Propagação PESO/QTDFIXADA + refresh global Comercial (v1.1.1)** (sessão Mai/2026, 2026-05-15) | ✅ Concluído · ⚠ Em testes em produção | **6 sprints Cat B na coerência Entrada↔Comercial↔Venda:** (B1) `salvar_vale_compra_banco` bloqueia salvar vale sem peso (filtro `GERAPRODUCAO='S'` antigo removido — causava bloqueio indevido em in natura); (B2) INSERT TGFITE TOP 13 grava `PESO = QTDFIXADA` da TOP 11 pra todos os itens do lote; (B3) `atualizar_peso_comercial_entrada` propaga alteração tardia da TOP 11 pra `TGFITE.PESO` de todos os itens da TOP 13 do lote — operador não precisa abrir vale; (B4) espelhamento bidirecional PESO↔QTDFIXADA na TOP 11 ao salvar vale; (B5) `inserir_item_nota_banco` espelha QTDFIXADA=PESO já no INSERT quando `GERAPRODUCAO≠'S'`; (B6) `upsert_preco_in_natura_modalFaturamento` propaga preço modal pra TOP 11 (PRECOBASE/VLRUNIT/VLRTOT + recalcular cabeçalho) em produtos in natura — só Comercial define preço, replica auto pra Entrada. **Refresh global Comercial**: `comercial.js` e `comercialFinanceiro.js` chamam `ComercialFiltros.atualizar()` imediato após sucesso (era setTimeout 500ms). Resolve bug "preço salvo mas card Entrada não atualiza". Tests novos em `test_salvar_vale_peso.py` (16 cobrindo B1-B6 + matriz de cenários). **Validação**: 124+ tests passam (suítes Rastreio/Comercial/Entrada/Combustível). |
| **Combustível B7+B8: hodômetro opcional + data + NUMNOTA do operador** (sessão Mai/2026, 2026-05-15) | ✅ Concluído · ⚠ Em testes em produção | **B7**: removida obrigatoriedade de hodômetro/horímetro em `criar_requisicao_combustivel_banco` (era forçada em INTERNA_FROTA), `editar_requisicao_combustivel_banco` (INTERNA_FROTA + EXTERNA_POSTO) e `criar_abastecimento_externo_banco`. Campo "Data" novo no modal de requisição interna (ID `reqDtNeg`) — usa data atual como default, payload `dtneg='YYYY-MM-DD'` formatado pra DD/MM/YYYY no TGFCAB.DTNEG/DTMOV. **B8 — NUMNOTA do operador no externo (EXTERNA_POSTO)**: campo `reqExternoDoc` virou `type=number` com validação `/^\d+$/` no frontend + validação numérica estrita no backend (texto "NF 12345" → erro `"deve ser apenas números (digite 12345, não NF 12345)"`). Operador digita o nº da NF/boleto — vai pra `TGFCAB.NUMNOTA` E `TGFFIN.NUMNOTA` (mesma variável `:numnota`, sem trava de colisão pq Sankhya aceita repetidos em TOP 53). Edição também propaga NUMNOTA pra TGFFIN. **DTVENC auto no externo**: nova função `_recalcularDtVencExterno` replica lógica da Entrada — listener no `reqExternoDtNeg.change` + `onSelect` do typeahead `reqTipVendaVis` calcula `DTVENC = DTNEG + BASEPRAZO` via endpoint `/combustivel/api/prazo-tipvenda/`. 4 tests novos. |
| **Rastreio B9: Pendente/Finalizado por completude do rastreio + agrupar lotes** (sessão Mai/2026, 2026-05-15) | ✅ Concluído · ⚠ Em testes em produção | **Backend Cat B**: `consultar_pedidos_abertos_para_atribuicao` trocou critério de STATUSNOTA por **completude do rastreio** (existem itens com `CODAGREGACAO IS NULL`?). Pendente = pedido com ao menos 1 item sem lote; Finalizado = todos itens vinculados. Param renomeado `mostrar_faturados → mostrar_finalizados` (alias retro preservado). Cobre TOP 34 (em aberto + STATUSNOTA='L') + notas órfãs TOP 35/37 sem TGFVAR/vínculo manual — **sem duplicação**: TOP 35/37 só entra como ÓRFÃ. **Frontend Lotes**: novo toggle `grpLotes` (Por Parceiro / Por Produto) espelhando o dos pedidos. `renderLotes` agora agrupa cards por NOMEPARC_ORIGEM ou DESCRPROD ordenado alfabeticamente. Header do grupo reusa visual `.pedido-bloco-header` (chevron + avatar + nome + qtd total verde + contador de lotes), mas sem impressora/percentual/NUNOTA. Cards recuados (margin-left 16px) e sem repetir nome do produto (já está no header). Persistência em `iagro:rastreio:prefs:v1`. **Frontend Pedidos**: label "Faturado" → "Finalizado", `mostrarFaturados → mostrarFinalizados` (retrocompat read em prefs). **Tests**: 77/77 rastreio passam — `test_toggle_status_apenas_faturados` renomeado pra `_apenas_finalizados`, + test novo `test_alias_retro_mostrar_faturados` confirma compat URL legada. |
| **Imagem de fundo nas páginas autenticadas** (sessão Mai/2026, 2026-05-15) | ✅ Concluído | `html-bg.png` em `static/sankhya_integration/` aplicada no `.app-content` com overlay branco-suave 92% (`linear-gradient(rgba(244,246,244,0.92)...)`) + `background-attachment: fixed`. Imagem fica sutil entre painéis; `.panel` brancos sólidos preservam legibilidade. Tela de login mantém imagem própria (`login-bg.png` no `home.css`). |
| **Defesa contra fakes em Oracle — incidente 803 registros (v1.1.3)** (sessão Mai/2026, 2026-05-18) | ✅ Concluído · ⚠ Em testes em produção | **Incidente detectado**: tela de Movimentações da placa NLM6688 mostrava 5 INTERNA_FROTA + 3 EXTERNA_POSTO de DIESEL S10 criados no MESMO SEGUNDO (`2026-05-17 23:06:28`), todos com NOMEUSU='Teste' CODUSU=1. **Causa raiz**: tests da suíte (`test_views_combustivel.py` etc.) injetam sessão fake `session['codusu']=1, session['nomeusu']='Teste'`. Quando rodados contra Oracle de produção, 7 funções do Combustível + `registrar_auditoria` + 7 funções de email/aliases **não tinham guard `verificar_permissao_escrita()`** antes do INSERT — escreviam fakes reais. **Universo do estrago**: 803 registros (8 TGFCAB TOP 53 + 8 TGFITE + 3 TGFFIN + 8 AD_REQUISICAO_COMBUSTIVEL + 776 AD_AUDITORIA_GERAL + 2 aliases CONFIRMADO_POR=1). **Limpeza executada (Cat B aprovada ponto-a-ponto)**: DELETE atômico em transação com pré-checks rigorosos (TGFVAR vazio, TGFFIN não baixado, NUMNOTA mock confirmado, DTALTER bate janela). 843 registros deletados no total (803 + 38 residuais que vazaram durante validações + 2 aliases). **Arquitetura de defesa em 2 camadas aplicada**: (1) **FÍSICA** em `obter_conexao_oracle` — `'test' in sys.argv` levanta RuntimeError; tests bem-mockados (`@patch('...obter_conexao_oracle')`) não chegam aqui, mal-mockados falham com erro visível; bypass intencional via `IAGRO_TEST_REAL_DB=true`. (2) **LÓGICA** via `IAgroTestRunner` ([tests/test_runner.py](../sankhya_integration/tests/test_runner.py), configurado em `settings.TEST_RUNNER`) — patcha `verificar_permissao_escrita` globalmente retornando True nos tests pra ~80 guards das funções de escrita não regredirem suíte. **Guards adicionados em 15 funções** que não checavam o flag: Combustível 7 (criar/editar/excluir requisicao + entrada + abastecimento_externo), Email 8 (inserir/atualizar/deletar pedido_email + vincular_nunota + buscar/gravar aliases + gravar_cod_cliente), Comercial 1 (atualizar_simulacao_item_banco), Auditoria 1 (registrar_auditoria), Notas Fiscais 1 (recalcular_totais_nota_banco — defesa em profundidade). **Validação**: 590 tests, 15 falhas pré-existentes (= baseline = zero regressão); zero novos fakes na suíte completa. Memory salva em `incidente_fakes_em_oracle_18_05_26.md`. |
| **Caixas — diagnóstico fórmula errada + tentativa AD_PESO_CAIXA_PRODUTO revertida** (sessão Mai/2026, 2026-05-18) | ✅ Diagnóstico + reversão concluídos | **Descoberta crítica de produção**: smoke na Nota 6361 UNIAO mostrou que o sistema calculou +3.188 caixas quando operador vendeu apenas 149 (104 cx tomate × 22kg + 45 cx cenoura × 20kg = 2.288+900 = 3.188 **kg**). **Conclusão**: na Agromil `CODVOL='CX'` em TGFITE **não significa "QTDNEG é número de caixas"** — QTDNEG está sempre em kg, mesmo em vendas marcadas CX. Fórmula híbrida anterior (CASE CX → QTDNEG direto) estava errada — removida. **Tentativa 1 (revertida)**: criamos tabela auxiliar `AD_PESO_CAIXA_PRODUTO` (peso default por produto) + aba dedicada + 3 endpoints + 3 funções service. Smoke validou Nota 6361 → 149 caixas após cadastro de tomate=22 e cenoura=20. **Reversão (mesmo dia)**: operador apontou que peso varia por LOTE (tomate 20 ou 22 dependendo do produtor) — cadastro fixo seria chute em ~50% dos casos. Real solução = forçar IAgro como fluxo único (Rastreio popula PESO real por linha). **Estado final**: fórmula `CEIL(QTDNEG / TGFITE.PESO)` apenas, sem fallback. Vendas legadas sem PESO ficam fora do cálculo automático — operador usa AJUSTE_SALDO pra clientes importantes (Assaí/Sendas). Conforme IAgro vira fluxo único, vendas novas trazem PESO via Rastreio. **UX bug colateral corrigido**: `.cx-detalhe-vazio[hidden] { display: none !important }` (atributo HTML hidden era sobrescrito por `display: flex`). **Layout compactado mantido**: header 1 linha, stats em pílulas horizontais (6 cards), timeline com `flex: 1 1 auto` ocupando ~80% do detalhe. **DROP AD_PESO_CAIXA_PRODUTO** + apaga SQL versionada + remove 3 funções service + 3 endpoints + aba + modal + 10 tests. **Tests**: 52/52, 642 total, 15 falhas pré-existentes (baseline = zero regressão). |
| **Caixas — botão Atualizar com backfill TEMPORÁRIO + banner "Processando pesos das notas"** (sessão Mai/2026, 2026-05-18) | ✅ Concluído · ⚠ Em testes em produção · ⏸ Pendente decisão Cat B (embalagem variável por produto×cliente) | **Função service `popular_pesos_top34_35_37_via_moda_TEMP`** (oracle_conn.py) faz MERGE idempotente em TGFITE TOP 34 (STATUSNOTA<>'E') + TOP 35/37 (STATUSNOTA='L') populando PESO=0 com a moda do PESO da TOP 26 por CODPROD. Endpoint `POST /sankhya/caixas/api/refresh-pesos/` chamado pelo botão Atualizar antes de recarregar saldo. **Banner sticky no topo da tela** (gradient indigo/verde com spinner) mostra "Processando pesos das notas…" enquanto roda. Toast verde com qtd atualizada no fim. **Backfill em massa rodado uma vez via script**: 136.641 linhas TOP 34 + 184.224 linhas TOP 35/37 atualizadas. Saldo de 148 clientes agora visível: CLIENTE PEDRA=329k, ASSAI PALMAS TEOTONIO=260k, UNIAO=258k, demais Assaís 100-220k. Volume reflete "histórico inteiro de saídas sem coletas registradas" pré-IAgro — operador precisa fazer AJUSTE_SALDO manual nos clientes principais pra refletir realidade física. **Marcadores `[TEMPORÁRIO Mai/2026]`** em 4 locais (oracle_conn.py + views.py + urls.py + caixas.js) com checklist de remoção quando IAgro virar fluxo único. **Tests**: 58/58 em test_caixas.py (6 novos cobrindo `RefreshPesosTempServiceTest` + `RefreshPesosEndpointTest`). **Decisão pendente registrada em modules/caixas.md → "Decisão pendente — embalagem variável por (produto × cliente)"**: operador relatou que REPOLHO+Palmas/Araguaína sai em caixa de madeira, ABÓBORA JAPONESA fora-DF sai em saco, enquanto outros vão em plástica. Cadastro atual é global por produto, não cobre. 3 caminhos propostos (A: cadastro produto×cliente, B: campo no modal Venda, C: tela bulk de expedição). Voltar nesse ponto pra decidir qual implementar. |
| **Módulo Caixas — controle de vasilhame retornável (Cat A + Cat B)** (sessão Mai/2026, 2026-05-18) | ✅ Cat A + Cat B em produção · ⚠ Em testes em produção | **Tela nova `/sankhya/caixas/`** pra controlar caixas plásticas retornáveis em campo. Decorator `'caixas': ['1','6','8','9','10','11']` (visível pra quase todos). **Modelo de saldo (final)**: `saldo = Σ saídas TOP 35/37 'L' − Σ devoluções TOP 36 'L' − Σ AD_COLETA(COLETA+QUEBRA+PERDA, ESTORNADO='N') + Σ AD_COLETA(AJUSTE_SALDO, ESTORNADO='N', sinal natural)`. Fórmula de caixas híbrida: `CASE WHEN CODVOL='CX' THEN QTDNEG ELSE CEIL(QTDNEG/PESO) END` — cobre vendas em caixa direta (17% legado) + vendas IAgro Mai/2026 com PESO populado. Filtra produtos PAPELAO de AD_PRODUTO_CAIXA (default PLASTICA quando sem cadastro). **DDLs aplicadas no Oracle**: `AD_COLETA_CAIXAS` (ID, CODPARC, QTD_CAIXAS com CHECK condicional ao motivo, DATA_COLETA, MOTIVO check COLETA/QUEBRA/PERDA/**AJUSTE_SALDO**, ESTORNADO soft-delete, audit completo) + `AD_PRODUTO_CAIXA` (CODPROD PK, TIPO_CAIXA check PLASTICA/PAPELAO, audit) + migration ampliando CK_AD_AUDIT_MODULO pra aceitar 'caixas'. **Backend Cat A**: 4 funções service de leitura — `consultar_saldo_caixas` (filtros q/codparc/apenas_saldo_positivo, agregado, breakdown de 5 categorias incluindo `caixas_ajuste`), `obter_timeline_caixas` (eventos cronológicos com LISTAGG produtos), `listar_coletas_caixas` (paginação ROW_NUMBER), `listar_produtos_caixa` (JOIN TGFPRO). Schema-resilient via `_existe_coluna`. **Backend Cat B**: `criar_coleta_caixas_banco` (INSERT atômico com validações + audit pós-commit; AJUSTE_SALDO permite qtd != 0 positivo ou negativo), `estornar_coleta_caixas_banco` (soft-delete ESTORNADO='S' + motivo), `upsert_produto_caixa_banco` (MERGE detectando INSERT vs UPDATE). Todas com `verificar_permissao_escrita()` guard. **8 endpoints REST** (5 GET + 3 POST). **Frontend**: template + CSS + JS (~900 linhas). Resumo topo (4 indicadores), 2 abas (Saldo / Tipo de caixa). Layout 2 colunas (lista 380px + detalhe com timeline). Timeline DESC com 6 tipos coloridos (verde saídas, ciano devolução/coleta, âmbar quebra, vermelho perda, **indigo ajuste**). Modal de Lançar coleta com 4º radio "Ajuste de saldo (excepcional)" + hint contextual permitindo negativo + observação obrigatória pra audit. Botão estornar inline por evento. Sidebar: item Caixas em Administrativo. **AJUSTE_SALDO**: cobre saldo inicial (caixa que já estava em campo antes do controle começar) E correção pontual quando saldo diverge da realidade. Cliente sem vendas mas com ajuste entra no resultado (caso "começou controle hoje com 80 caixas no Assaí"). **Tests**: 50/50 em `test_caixas.py` (acesso por 6 grupos, 4 services + 3 cenários de AJUSTE_SALDO, 4 endpoints GET + 4 POST, fluxo INSERT/UPDATE/estorno, schema-resilient). Smoke real validou fórmula no Oracle de produção. 640 tests total na suíte, 15 falhas pré-existentes (= baseline = zero regressão). |
| **Combustível — financeiro automático pra terceiros + Diesel/ARLA separados + filtro de datas + UX** (sessão Mai/2026, 2026-05-26) | ✅ Concluído · ⚠ Em testes em produção | **Sessão grande do Combustível.** **B1 + B2 + B3 (Cat B aplicadas)** entregam ciclo completo de TGFFIN automático pra veículos de terceiro. Detecção via `TGFVEI.PROPRIO='N'` (não mais tipo `EXTERNA_FRETE` na UI). **Backend**: nova fn `zerar_fracao_lote_banco` no Rastreio (TOP 33, tolerância 1%); modificações em `criar_requisicao_combustivel_banco` (gera TGFFIN auto receita CODNAT 10040800 + CODCENCUS 10100 + RECDESP +1 + DTVENC decendial 1/10/20/fim-mês), `editar_requisicao_combustivel_banco` (idempotência 5 cenários A/B/C/D/F com trava NURENEG), `excluir_requisicao_combustivel_banco` (DELETE TGFFIN universal + trava NURENEG); novas funções aditivas: `proxima_data_fechamento_decendial` (helper Python puro), `consultar_tgffin_renegociado` (trava `TGFFIN.NURENEG IS NOT NULL` — confirmado via smoke real). **HISTORICO TGFFIN padronizado**: `"Abastecimento Diesel S10 / 50 LT (R$6,26) - JFO5H79"` (combustível + qtd + preço + placa). **Frontend Cat A**: tipo `EXTERNA_FRETE` removido da UI; campo VLRUNIT editável (era readonly em internos); **filtro de datas no card Movimentações** (ini/fim + `<<`/`>>` setas + default mês atual dia 1→hoje), dataIni replica em dataFim sempre, vale pra listagem geral E pro detalhe do veículo; filtro Status removido; **campo pesquisa de veículos** (placa/modelo/parceiro) na mesma linha do header + toggle Frota/Maquinário; **lista de veículos expandida** até a margem inferior do card (era max-height 320px fixo); **média de consumo Diesel vs ARLA separada** — ARLA (codprod 1374) não interfere mais no km/L do Diesel (392, 1373). Resumo do detalhe ganha cards Diesel total/ARLA total/Consumo Diesel/Consumo ARLA/ARLA-Diesel% (esperado 3-5%). **Padrão data ini/fim + << >>** documentado em [`conventions.md`](conventions.md) → "Período data inicial → data final + navegação dia-a-dia". **Pré-req operacional**: cadastrar veículo terceiro em TGFVEI com PROPRIO='N' + CODPARC. **Tests**: 125/125 OK no Combustível (incluindo 9 novos do B1 caminho terceiro, 6 do B2 cenários A/B/C/D/F, 4 do B3, 6 do consumo Diesel/ARLA separado, helper decendial, consultar_tgffin_renegociado). **Documentação**: `CLAUDE.md` → "⛽ Combustível — financeiro automático pra terceiros"; [`modules/combustivel.md`](modules/combustivel.md) → "TGFFIN automático pra veículos de terceiro" + "Filtro de datas movido pro card Movimentações" + "Média Diesel vs ARLA". |
| **Rastreio — Zerar fração do lote via TOP 33 manual** (sessão Mai/2026, 2026-05-26) | ✅ Concluído · ⚠ Em testes em produção | **Problema operacional real**: pedido pede 19 kg, operação envia 20 kg (caixa cheia), sistema desconta 19 → lote fica com 1 kg fantasma no saldo. Tela poluída com restos que não existem fisicamente. **Solução**: botão **broom** no card de lote quando saldo ≤ 1% × `qtd_entrada` (TOP 11 origem). Click → modal de confirmação → cria TGFCAB **TOP 33 — AVARIA DE AJUSTE** (CODNAT 20010200, mesmo da TOP 30; smoke confirmou TOP 33 já existe e foi usada 269× pelo Sankhya nativo) + TGFITE com `CODAGREGACAO` preservado (rastreabilidade — diferente do uso nativo Sankhya que deixa NULL). VLRUNIT do vale TOP 13 lido pra documentar custo da perda. Tolerância adaptativa **1% naturalmente proporcional** ao volume — lote de 1000 kg → 10 kg; lote de 50 kg → 0,5 kg. **Trava em backend** (defesa em profundidade): se operador tentar zerar lote com saldo acima da tolerância, recusa com mensagem `"Saldo X kg está acima da tolerância (Y kg). Use TOP 30 avulsa pra avaria maior."`. **Manual, não automático**: operador decide caso a caso (audit ZERAR_FRACAO_LOTE registra quem zerou). Mais explícito + reversível mentalmente nos primeiros dias. **Componentes**: função `zerar_fracao_lote_banco` (Cat B), endpoint `POST /sankhya/rastreio/api/zerar-fracao/`, badge `📦 fração` âmbar + classe `.lote-fracao` no card, botão `ph-broom` no rastreio.js, função `zerarFracaoDoLote(lote)` com `IAgro.confirmarAcao`. **CODNAT_POR_TOP[33]=20010200** adicionado ao dict canônico em `oracle_conn.py`. **Tests**: 12 novos (7 service + 5 endpoint). **Documentação**: `CLAUDE.md` → "🧹 Rastreio — Zerar fração do lote via TOP 33"; [`modules/rastreio.md`](modules/rastreio.md) → "Zerar fração do lote (TOP 33)"; [`dependencias_sankhya.md`](dependencias_sankhya.md) atualizado com TOP 33 e CODNAT 10040800 (B1 Combustível). |
| **Rastreio — fix SPLIT (RESERVA/AE/USOPROD) + filtro cliente_q + busca unificada (q_lotes) + remoção dos checkboxes + cleanup pedido fantasma** (sessão Mai/2026, 2026-05-25) | ✅ Concluído · ⚠ Em testes em produção | **3 fixes + 1 feature + 1 cleanup**. **(1) Fix SPLIT do Rastreio**: `atribuir_lote_item_pedido` quando faz SPLIT (atribuição parcial — qtd < QTDNEG) criava linha nova via `INSERT INTO TGFITE ... SELECT ...` listando só 12 colunas — `RESERVA`, `ATUALESTOQUE`, `USOPROD` ficavam com default Oracle (`'N'`/`-1`/`NULL`) e Sankhya cortava o item silenciosamente na emissão da NFe. Mesma raiz do fix 2026-05-20 em `inserir_item_nota_banco`, mas no caminho de split do Rastreio. Fix: INSERT força `RESERVA='S'`, `ATUALESTOQUE=1`, `USOPROD=NVL((SELECT FROM TGFPRO), 'R')`. **Backfill em produção**: 48 linhas TGFITE corrigidas via UPDATE direto (faixa A pré-fix + B 20-24/05 + C 25/05). NUNOTA 113155 SEQ 34 (SALVIA cortada — caso real do cliente) incluída. **(2) Fix filtro cliente_q quebrado**: refator 2026-05-19 trocou `FROM SANKHYA.ANDRE_IAGRO_SALDO_LOTE` (view) pra `SANKHYA.AD_SALDO_LOTE_CACHE` (tabela materializada) em `consultar_saldo_lote_disponivel`, mas o EXISTS do filtro `cliente_q` ainda referenciava `ANDRE_IAGRO_SALDO_LOTE.CODPROD` → Oracle `ORA-00904: invalid identifier` silencioso (toast no frontend, painel vazio). Fix de 1 linha: trocar pra `AD_SALDO_LOTE_CACHE.CODPROD`. **Lição registrada em gotchas**: ao refatorar FROM, grep todas as referências qualificadas (EXISTS/NOT EXISTS/IN). **(3) Busca unificada do Rastreio**: refatoração de 2 campos antigos pra **2 campos únicos** — `#filtroPedidos` (texto → NOMEPARC LIKE; dígito → NUNOTA OR NUMNOTA; cross-filter nos lotes via `cliente_q` que aceita ambos) e `#filtroLotes` → param `q_lotes` (texto → CODAGREGACAO + DESCRPROD + NOMEPARC_ORIGEM LIKE; dígito → + NUNOTA_ORIGEM exato; cross-filter nos pedidos). Removidos `#filtroFabricante`/dropdown + `fabricantesCache`/`startFabricantesCache`/`typeaheadFabricante`/`buscarFabricantes` (~90 linhas JS). Cross-filter `q_lotes` em Pedidos pré-resolve CODPRODs em 1 SELECT separado + IN literal — **30× speedup** (28-34s → 1.08-1.18s). **(4) DELETE pedido fantasma**: NUNOTA 113508 (TOP 34, R$ 74.485, ASSAI PALMAS CESAMAR, 18 itens) existia no Oracle mas Sankhya não exibia (NUMNOTA=NUNOTA=113508 igual confundia filtro nativo). DELETE em cascata via `excluir_nota_completa_banco` + audit `EXCLUIR_PEDIDO` em AD_AUDITORIA_GERAL. **(5) Remoção dos checkboxes de filtro cruzado** (Lotes + Pedidos): operador relatou que atrapalhavam mais que ajudavam. Removidos: 6 checkboxes HTML + 5 event listeners + 2 sets de estado (`checksLotes`, `checksPorPedido`) + 7 funções helper (`_uniaoFiltrosProdutos`, `temFiltroProdutos`, `loteEstaCheckado`, `produtoLinhaEstaCheckada`, `produtoEstaFiltrado`, `_limparTodosChecks`, `aplicarFiltroProdutos`, `toggleCheckLote`, `toggleCheckProdutoPedido`, `toggleFiltroProduto`, `setsIguais`). Mantido `pedidoIsolado` + adicionado `codprodsIsolados` (Set<int>) pra cross-filter de lotes via click no header do pedido. Cross-filter agora vive só nos campos únicos `q_lotes` / `cliente_q` automaticamente. **Tests novos**: `test_split_insert_inclui_reserva_atualestoque_usoprod` + `ConsultarSaldoLoteClienteQTest` (×2) + `ConsultarPedidosAbertosBuscaTest` + `QLotesUnificadoTest` (×3) = 7 tests novos. Suíte rastreio 84 tests, 5 falhas pré-existentes (baseline). **Compat**: `q_lote_prod` e `fabricante` continuam aceitos no backend (Classificação ainda usa `fabricante` — endpoint diferente). **Documentação**: `CLAUDE.md` → "🧬 Rastreio — fix SPLIT + filtro cliente_q quebrado" + "🔍 Rastreio — busca unificada"; [`modules/rastreio.md`](modules/rastreio.md) → seção Filtros atualizada + tabela de funções service com q_lotes; [`gotchas.md`](gotchas.md) → "Mesmo bug no SPLIT do Rastreio" + "Backfill aplicado em produção" + "Filtro `cliente_q` quebrado pós-refator do cache de saldo". Pendente: restart NSSM no servidor pra código novo entrar em vigor. |
| **Faturamento Caminho C — TGFCAB nova + TGFVAR par pedido↔nota + herança fiscal** (sessão Mai/2026, 2026-05-22 continuação) | ✅ Concluído · ⚠ Em testes em produção | **Pivotagem arquitetural do faturamento.** **Smoke A1 (investigação)**: criar TGFCAB TOP 35 via Oracle direto + `STP_CONFIRMANOTA2` → STATUSNOTA='L', mas módulo Java do Sankhya **não emite NFe** (Sankhya retorna "Documento NNNNN: Nota sem status nfe. Ignorada na impressão"). Módulo Java processa apenas notas criadas pela rotina nativa de faturamento — exige dezenas de campos fiscais (CFOP, CODTRIB, IDALIQICMS, NUTAB, NATUREZAOPERDES, INDNEGMODAL, TPRETISS, CODCIDORIGEM/DESTINO, CODUFORIGEM/DESTINO, CLASSIFICMS) que rotinas Java internas populam — inviável replicar via SQL. **Pivot pra caminho C**: `faturar_pedido_venda_banco` reescrita (Cat B grande) — antes fazia `UPDATE TGFCAB SET CODTIPOPER=35/37` (in-place, pedido sumia), agora **cria TGFCAB NOVA + TGFITE espelhado + TGFVAR pareando pedido↔nota**. Pedido TOP 34 fica intacto (STATUSNOTA='L'). **Fluxo final**: IAgro cria pedido → IAgro Faturar (cria TGFCAB nova STATUSNOTA='P') → Sankhya CONFIRMAR (módulo Java emite NFe SEFAZ). **Confirmação implícita**: se pedido está STATUSNOTA='P', UPDATE pra 'L' antes de criar nota (1 clique unificado). **Trava de lote removida**: backend `logger.info` em vez de bloquear; frontend mostra aviso amarelo informativo (botão FATURAR sempre habilitado). **NUMNOTA condicional**: TOP 35 → 0 (Java sobrescreve na emissão SEFAZ); TOP 37 → NUNOTA (sem emissão fiscal). **RESERVA/ATUALESTOQUE pra TOP 35/37**: `'N'`/`-1` no UPDATE pós-INSERT (semântica venda; diferente de TOP 34 que reserva). **Herança fiscal**: busca última TOP 35/37 STATUSNFE='A' do mesmo (CODEMP, CODPARC, CODPROD) e copia NATUREZAOPERDES/INDNEGMODAL/TPRETISS/CLASSIFICMS/INDPRESNFCE (TGFCAB) + CODCFO/CODTRIB/IDALIQICMS/NUTAB/ORIGPROD/PRODUTONFE/GTINNFE/GTINTRIBNFE (TGFITE) — NVL preserva valores não-nulos. Sem isso, Sankhya rejeita emissão com `CORE_E04895` (natOp/idDest/CFOP inválidos). **Bug `TRG_UPD_TGFCAB`** descoberto + documentado: ao mudar CODTIPOPER (caminho antigo), trigger exige trio `(CODTIPOPER, DHTIPOPER, TIPMOV)` sincronizado — sem isso `ORA-20101: Tipo de operação não esta ativo` ou `Esta TOP X não pode ser lançada nesta opção`. TOP 34=TIPMOV='P', TOP 35/37=TIPMOV='V'. **Bug visual UI corrigido**: endpoint `/sankhya/item/list/` mapeava `'qtd': qtdconferida`, mas fix B4 zerou QTDCONFERIDA em TOP 34/35/37 — UI mostrava QTD=0. Fix: `'qtd': qtdneg` + `'qtd_conferida': qtdconferida` separados. **Validação**: 21 tests passam (FaturarPedidoVendaTest + ConfirmarPedidoVendaServiceTest + ConfirmarPedidoVendaEndpointTest). **Documentação**: `CLAUDE.md` → "📑 Faturamento Caminho C"; [`modules/venda.md`](modules/venda.md) → "Faturamento — `faturar_pedido_venda_banco` (Caminho C)"; [`gotchas.md`](gotchas.md) → "Trigger TRG_UPD_TGFCAB exige trio CODTIPOPER+DHTIPOPER+TIPMOV" + "Módulo Java Sankhya não emite NFe de TGFCAB criada via Oracle direto". |
| **Fix `QTDCONFERIDA=0` em TOP 34/35/37 + Botão CONFIRMAR pedido no portal Venda** (sessão Mai/2026, 2026-05-22) | ✅ Concluído · ⚠ Em testes em produção | **Sessão de diagnóstico + 2 entregas.** **Bug original**: pedidos IAgro sem lote vinculado não conseguiam ser faturados pelo Sankhya — erro `[CORE_E04678] Não existem produtos/quantidades disponíveis`. **Primeira hipótese (errada)**: PENDENTE='S' (commit `29bfa59`, revertido em `b826024`). Sankhya nativo grava PENDENTE='S' em pedido TOP 34 não-atendido — IAgro estava correto. Lição registrada: validar premissas com pedidos do mesmo estado (atendido vs não-atendido) antes de inferir causa. **Causa real**: `inserir_item_nota_banco` gravava `QTDCONFERIDA = QTDNEG` por default (`qtdconferida = dados.get('QTDCONFERIDA') or qtdneg`). Sankhya interpretava como "item já conferido/entregue" → nada a atender. Diagnóstico cirúrgico via TGFITE 113264 (IAgro erro) vs 113259 (Sankhya nativo OK, mesmo cliente/produto/dia/STATUSNOTA/PENDENTE) — única coluna divergente. UPDATE cirúrgico em 113264 (QTDCONFERIDA: 1.0 → 0.0, sem mexer mais nada) destravou. **B4 (Cat B aplicada)**: default condicional ao CODTIPOPER em `inserir_item_nota_banco` — TOP 34/35/37 → `0.0`; outros TOPs preservam `QTDNEG`. Payload com QTDCONFERIDA explícita continua respeitado (inclusive `0` em TOP 11 — `or qtdneg` antigo ignorava 0). 4 tests novos. **B5 (Cat B aplicada — botão CONFIRMAR)**: pedido IAgro nasce em `STATUSNOTA='P'`; antes precisava clicar CONFIRMAR no Sankhya pra virar `'L'` antes de faturar. Botão novo `#btnConfirmarVenda` no portal Venda elimina fricção — UPDATE TGFCAB.STATUSNOTA='L' direto via `confirmar_pedido_venda_banco`. Endpoint `POST /sankhya/venda/api/confirmar/`, audit `CONFIRMAR_PEDIDO`, ícone Phosphor `ph-check-circle` verde Agromil, habilitado só pra TOP 34 STATUSNOTA != 'L'. SELECT da `listar_vendas_paginado` ganhou `c.STATUSNOTA` (Cat A) pra UX correta. 12 tests novos (6 service + 6 endpoint). **Fluxo final**: IAgro criar pedido → IAgro **✓ Confirmar** → Sankhya **Faturar**. Backward-compat 100% — operador pode continuar confirmando no Sankhya se preferir. **Suíte venda**: 157 tests, zero regressão (5 falhas baseline pré-existentes). **Memory salva**: `feedback_smoke_select_sem_aprovacao.md` — SELECT contra Oracle real pode rodar direto sem aprovação ponto-a-ponto (só escritas exigem). **Documentação**: `CLAUDE.md` → "🛠 Fix QTDCONFERIDA — CORE_E04678" + "✅ Botão Confirmar Pedido no portal Venda"; [`modules/venda.md`](modules/venda.md) → "Fix QTDCONFERIDA" + "Confirmar pedido — confirmar_pedido_venda_banco"; [`gotchas.md`](gotchas.md) → "QTDCONFERIDA=QTDNEG em TOP de venda quebra atender pedido". |
| **Impressão de pedidos PDV + Consolidação + Cascata de caixas em 3 camadas + Diretriz `AD_*` em tabelas nativas** (sessão Mai/2026, 2026-05-21) | ✅ Concluído · ⚠ Em testes em produção | **Sessão grande com 3 frentes.** **(1) Impressão de Pedidos** — habilitado botão impressora da Venda (`btnPrintVenda`, antes desabilitado). Modal `#impressaoModal` com 2 painéis (lista de pedidos com checkbox + consolidação por CODPROD). **PDF individual** (1 página/pedido, layout Sankhya idêntico — empresa+cliente+tabela `CÓDIGO/DESCR/UN/QTD/VLR UNIT/CX` + obs/totais) e **PDF consolidado** (agregado por produto com `QTD TOTAL/CAIXAS/Nº PEDIDOS`). **Chips dinâmicos** detectam grupos do dia automaticamente — só aparecem Assaí DF/Palmas+Araguaína/Todos Assaís/Economart/Exal/JC/Verdi/Na Horta quando há pedidos. Default ao abrir = "Todos do dia". Endpoints aceitam **POST JSON** pra evitar URI Too Long. **Linha de TOTAIS destacada** (verde Agromil + borda 1.8pt + 7mm altura + gap 1.5mm/5mm). QTD sem decimal, separador de milhar em tudo. **(2) Cascata de cálculo de caixas em 3 camadas em 1 query única** — `consultar_pesos_referencia_por_codprods` com CTEs unidas via `UNION ALL` + `MAX KEEP DENSE_RANK FIRST ORDER BY PRIO`: **PRIO 1 = Moda PESO TOP 26** (peso real classificado, mais preciso) · **PRIO 2 = `TGFVOA[CODPROD, 'CX', M].QUANTIDADE`** (cadastro Sankhya nativo de unidade alternativa — NOVO) · **PRIO 3 = Moda PESO TOP 11**. Cobertura via TGFVOA na Agromil: 62% dos produtos vendidos com `CODVOL='CX'` cadastrado (cobre GRAPE 372→20kg/CX e MILHO VERDE 61→10bd/CX após cadastro do operador); 97% somando outras unidades alt. Helper `_coletar_pesos_fallback_pdf` chama a função só pra CODPRODs sem `qtd_caixas` (custo zero quando todos têm PESO). **(3) Diretriz arquitetural revisada (2026-05-21)** — colunas `AD_*` em tabelas Sankhya nativas agora **permitidas** (com aprovação Cat B). Antes proibido. Justificativa: spin-off vai replicar schema Sankhya exato + extensões `AD_*`, sem dívida adicional. **Disciplina**: prefixo `AD_*` obrigatório, ler antes de criar, DDL versionada, Cat B obrigatório, tabela auxiliar ainda preferível quando dado é multi-row. **Componentes**: novo módulo [pedido_venda_pdf.py](../sankhya_integration/services/pedido_venda_pdf.py) (reportlab); 4 endpoints REST sob `/sankhya/venda/api/imprimir/`; modal HTML+JS+CSS (~700 linhas); funções service Cat A `obter_dados_pedido_completo_para_impressao`, `listar_pedidos_para_impressao`, `consultar_pesos_referencia_por_codprods`. **Bug crítico durante implementação**: `WHERE STATUSNOTA <> 'E' OR STATUSNOTA IS NULL` sem parênteses → precedência do OR fazia filtros de data serem ignorados (14k pedidos retornavam). Fix: adicionar parênteses. **Modal de itens da Venda também recebeu polish**: chips Tabela/Promoção inline alinhados aos inputs (bug `align-items: stretch` por `.align-end` sem regra CSS — força `align-items: flex-end !important`); chip Manual + campo Motivo removidos (desburocratização); botões +/X sem bordas, + verde maior, X virou borracha `ph-eraser` vermelha; modal 92vh com scroll-panel adaptativo; tfoot na tabela de itens com totais (qtd+valor) sticky + separador de milhar. **Documentação**: `CLAUDE.md` → "🖨 Impressão de pedidos PDV + Consolidação por grupo" + "⚙ Diretriz arquitetural revisada"; [`modules/venda.md`](modules/venda.md) → "Impressão de pedidos"; [`dependencias_sankhya.md`](dependencias_sankhya.md) §1.7.5 (TGFVOA) + §5.5 (inventário colunas `AD_*` em tabelas nativas); [`rules.md`](rules.md) regra #2 (esclarecimento ALTER TABLE com `AD_*`). |
| **Tabela de Preços + Promoções com escopo flexível** (sessão Mai/2026, 2026-05-20/21) | ✅ Concluído · ⚠ Em testes em produção | **2 entregas grandes em 2 dias.** **Dia 1 (2026-05-20)**: 1ª versão de Promoções com `CODPARC` obrigatório + tela CRUD em `/sankhya/venda/promocoes/` + chips Tabela/Promoção/Manual no modal de item da Venda + `AD_ITEM_PRECO_ORIGEM` (TABELA/PROMOCAO/MANUAL — MANUAL exige observação obrigatória). **Dia 2 (2026-05-21)** — refator pra **escopo flexível**: `ALTER AD_PROMOCAO` adicionou `CODTAB` nullable + tornou `CODPARC` nullable + `CHECK XOR`. Promoção pode ser por **grupo (CODTAB do Sankhya, ex: 5=ASSAI afeta 7 lojas Assaí DF + 2 BARÃO)** OU **parceiro individual**. **`consultar_promocoes_vigentes`** ganhou OR no WHERE (CODPARC direto OU CODTAB do parceiro). **Sidebar nova section "Tabela de Preços"** entre Comercial e Administrativo com 2 sub-itens: `Tabela` (visualização leitura — `/sankhya/venda/tabela-precos/`) e `Promoções` (CRUD migrado de Administrativo). **Tela Tabela**: sidebar lista TODAS as TGFNTA do Sankhya (parte de TGFNTA com LEFT JOIN TGFPAR) ordenadas por nome com toggle "Mostrar inativas". Conteúdo mostra clientes do grupo (chips), preços TGFEXC + flag promoção vigente (linha amarela). **Descoberta TGFNTA** — Sankhya armazena nome humano da tabela em `TGFNTA.NOMETAB` (não em TGFTAB que só tem versionamento). View `VGFTAB = TGFNTA INNER JOIN TGFTAB MAX(DTVIGOR)`. Mapeamento real: CODTAB 5→ASSAI (9 lojas), 17→ASSAI ARAGUAINA (1), 18→ASSAI PALMAS (2), 6→ECONOMART (3), 15→EXAL (3), 4→JC (4), 10→VERDI (4), 2→NA HORTA (2) + 9 inativas (Atacadão Estrela, Camara, etc). **DDLs aplicadas**: AD_PROMOCAO + AD_ITEM_PRECO_ORIGEM + SEQ_AD_PROMOCAO + 2 índices + ALTER do dia 2. **Services novas (Cat A + B)**: `listar_tabelas_grupos(incluir_inativas)`, `listar_precos_da_tabela`, `consultar_promocoes_vigentes`, `listar_promocoes_cadastradas`, `consultar_origem_preco_item`, `criar/editar/excluir_promocao_banco`, `registrar_origem_preco_item`. **Integração**: `api_salvar_item_venda` + `api_atualizar_item_venda` aceitam `preco_origem`, `nutab`, `promocao_id`, `observacao_preco` no payload. **10 endpoints REST**. **Validação smoke**: Asa Norte (CODPARC=244, CODTAB=5) acha promoção CODTAB=5; Palmas (CODTAB=18) NÃO acha (CODTAB diferente); SEM escopo + COM 2 escopos rejeitados; conversão data Oracle `TO_DATE(:d, 'DD/MM/YYYY' \| 'YYYY-MM-DD')` explícito. **Documentação**: `CLAUDE.md` → "🎁 Tabela de Preços + Promoções com escopo flexível"; [`modules/venda.md`](modules/venda.md) → "Promoções com escopo flexível" + "Tela Tabela" + "Descoberta TGFNTA"; [`tabela_precos_sankhya.md`](tabela_precos_sankhya.md); [`dependencias_sankhya.md`](dependencias_sankhya.md) §1.4.4 (TGFNTA); [`schema.md`](schema.md) §7.9. |
| **Avaria fornecedor não-classif. + Toggle Absorver/Descontar com TOP 30 automática** (sessão Mai/2026, 2026-05-19/20) | ✅ Concluído · ⚠ Em testes em produção | **Fluxo end-to-end Entrada → Comercial pra avaria do fornecedor em itens não-classificáveis.** **Entrada (TOP 11)**: nova coluna "Avaria forn." no modal de itens com **auto-save no blur/Enter** (sem botão dedicado, feedback visual borda âmbar/verde/vermelha), só visível em `GERAPRODUCAO ≠ 'S'`. Endpoint dedicado `POST /sankhya/compras/api/avaria-fornecedor/` escapa da trava de edição quando vale já existe. **Funções**: `consultar_avarias_fornecedor_da_nota` + `consultar_avarias_fornecedor_de_pedido` (leituras) + `atualizar_avaria_fornecedor_naoclass` (escrita com trava **B10** — bloqueia se vale tem TGFFIN, msg humanizada). **Comercial (modal Faturamento)**: ⚠ ícone âmbar no nome do produto com avaria + mini-modal explicativo (qtd entrada, fornecedor, data). **Segmented control** sem ícones `[ Absorver \| Descontar ]` por item. **Default = Descontar** quando há `AD_QTDAVARIA > 0` na TOP 11 (operador da Doca já registrou = cobrar fornecedor é caso comum). Absorver é decisão explícita. **Motivos da trava do FATURAR explícitos abaixo do botão** (lista amarela com ⚠): vale não salvo, sem preço, classificação não finalizada. **Cat B implementadas (7 funções)**: `upsert_avaria_top30_lote` (B6 — cria TGFCAB TOP 30 com `AD_NUMPEDIDOORIG = NUNOTA da TOP 11`, herdando `CODTIPVENDA` da TOP 11 — trigger `TRG_INC_TGFCAB` exige; DELETE+INSERT TGFITE idempotente; VLRUNIT/VLRTOT documentando custo); `remover_avaria_top30_lote` (B7); `reconciliar_avaria_top30_no_faturamento` (B8 — orquestrador; sincroniza vale TOP 13 via B11 + TOP 30 com decisão final do toggle); `gerar_financeiro_banco(... lotes_absorver_avaria, codusu, nomeusu)` (B9 — chama B8 ANTES do INSERT TGFFIN, backward compat); `atualizar_avaria_fornecedor_naoclass` (B10 — trava de faturado); `upsert_preco_in_natura_modalFaturamento(... absorver_avaria_no_vale=True)` (B11 — quando `False` grava qtd LÍQUIDA no vale + vlrTotal recalculado); `alternar_modo_avaria_vale_lote` (B12 — endpoint dedicado `POST /comercial/api/avaria-modo-vale/` reaplica B11 sem precisar editar preço). **Reconciliação idempotente cobre refaturar**: faturar Absorver → desfaturar → mudar Descontar → refaturar = vale vira líquido + TOP 30 removida automaticamente. **TGFITE TOP 11 (Entrada) NUNCA modificada** — preserva documento original do recebimento. **Performance do modal**: refator `_renderListasFaturamento` (separação fetch×render), fetches em paralelo via `Promise.all` (avarias + cabeçalho + N items), cache local `cacheVale` evita re-fetch de lote repetido. `~4s → ~1s`. Ao faturar/desfaturar, helper `_aplicarEstadoFaturado(faturou)` atualiza botão+carimbo **na hora** após API responder; refresh do modal vira fire-and-forget. **Documentação**: `CLAUDE.md` → seção "♻ Avaria do fornecedor — Absorver/Descontar com TOP 30 automática"; [`modules/comercial.md`](modules/comercial.md) → "Toggle Descontar/Absorver com avaria interna automática"; [`modules/entrada.md`](modules/entrada.md) → "Avaria do fornecedor em item NÃO-classificável"; gotcha sobre CODTIPVENDA exigida em TOP 30 em `gotchas.md`. |
| **Rastreio — performance + travas Classif/Comercial + bugs + padronização visual** (sessão Mai/2026, 2026-05-19) | ✅ Concluído · ⚠ Em testes em produção | **Rodada grande**. **Performance (definitiva)**: (a) tabela materializada `SANKHYA.AD_SALDO_LOTE_CACHE` (DDL Cat B aplicada — 19 colunas espelhando view + ATUALIZADO_EM + 4 índices) com refresh por Windows Task Scheduler a cada 5 min (`python manage.py refresh_saldo_lote`); (b) `consultar_saldo_lote_disponivel` aponta `FROM AD_SALDO_LOTE_CACHE`; (c) cache Django LocMem 60s + versionamento + invalidação automática em 6 escritas (atribuir, desvincular, vínculo manual, retroativo, remover vínculo, refresh manual); (d) `consultar_pedidos_abertos_para_atribuicao` refatorado com **WITH clause `notas_pedido`** consolidando UNION ALL de TGFVAR + AD_VINCULO_PEDIDO_NOTA em 1 passada (era 5 subqueries correlacionadas por linha) + LEFT JOIN agregado de origem do lote trocado pra cache; (e) endpoint manual `POST /api/refresh-saldo/` no botão Atualizar. Resultado: lotes **12s → 80ms**, pedidos **14.5s → 590ms**, tela total **25s → 700ms** (35× mais rápido). Coerência: defasagem máx 5 min, lock pessimista valida na view real. **Travas Classif/Comercial**: (1) Classificação só bloqueia se existe TGFITE TOP 13 (sem TOP 35/37, sem filtro STATUSNOTA) — em 3 lugares: abertura modal + salvar + excluir item; (2) Comercial faturar exige preço **E** classificação finalizada (TGFCAB TOP 26 PENDENTE='N') em produtos classificáveis — frontend mostra 2 tooltips discriminados; backend `gerar_financeiro_banco` valida via NOT EXISTS em profundidade; botão FATURAR começa `disabled` com tooltip "Verificando travas..." (só libera após loop completo). **Bug fixes**: (a) descarte não excluía (`api_update_descarte_lote` filtrava `GERAPRODUCAO='S'` sem TOP → pegava linha errada; fix força `c.CODTIPOPER=11`); (b) cabeçalho do modal de classificação mostrava cliente da venda (MAX(NUNOTA) sem TOP); fix força TOP 11 em ambas queries; (c) filtro fornecedor não limpava ao remover chip (case 'fabricante' limpava input errado — resquício do split de Mai/2026); (d) aplicar simulação Comercial com Médio vazio zerava Médio (`ratio_medio = 0`); fix: usa default 0.5 quando `vMd === 0`. **Padronização visual + compactação**: linhas selecionáveis (lotes, pedidos POR PARCEIRO, POR PRODUTO) viraram formato tabela único (sem border lateral, só border-bottom como separador, padding 1px 8px, font-size 11px, `border-bottom: none` no last-child); avatares removidos dos ITENS (mantidos só nos headers de grupo); headers de grupo padronizados (padding 2px 10px, margin 0, gap 2px no container — total 30px por linha); toggle "Todos/Classific./Não-class." removido; agrupamento default agora **Por produto** (lotes e pedidos); `mostrarFinalizados=false` fixo no boot (não persiste — corta ~30% do tempo de query); +/- nos lotes (agrupar/desagrupar tudo). **Documentação consolidada**: [`CLAUDE.md`](../CLAUDE.md) → "🚀 Performance do Rastreio" + "🔓 Travas Classificação ↔ Comercial revisadas" + "Bug fixes pontuais"; [`modules/rastreio.md`](modules/rastreio.md) → "Materialização do saldo" + "Padrão de compactação visual" + "Defaults da UI"; [`modules/classificacao.md`](modules/classificacao.md) → "Cabeçalho do modal" + "Trava de edição quando lote foi pro Comercial"; [`modules/comercial.md`](modules/comercial.md) → "Travas do botão FATURAR". |
| **Sidebar agrupada retrátil + Hub Configurações + Tela Usuários (Cat A)** (sessão Mai/2026, 2026-05-17/18) | ✅ Concluído · ⚠ Em testes em produção | **Sidebar reorganizada** por departamento operacional: Painel (topo) + 4 ativos (Packing House: Entrada/Classif/Rastreio; Comercial: Comercial/Relatórios; Administrativo: Venda/Importação; Frota: Combustível) + 3 placeholders esmaecidos (Financeiro/DP-RH/Produção — opacity 0.5, "Em breve", não-clicáveis). **Acordeão**: 1 section aberta por vez (`localStorage:iagro:sidebar:section:v1`). Departamento do módulo ativo abre automaticamente via script inline pré-paint. **Engrenagem ⚙ no header** (visível só pra grupos 1+6) → `/sankhya/configuracoes/` — hub com cards Usuários + Auditoria (Auditoria removida da sidebar). **Tela Usuários** `/sankhya/usuarios/` Cat A: backend com 3 funções aditivas (`listar_usuarios` paginado com filtros busca/grupo/inativos, `consultar_usuario_detalhe` com grupos extras TSIGPU, `consultar_grupos_disponiveis` TSIGRU). 8 endpoints (3 leitura + 6 stubs Cat B que retornam 501 `pendente_cat_b=true`). Layout 2 colunas (lista 360px + detalhe), modais de cadastro/add grupo, badges (ATIVO/INATIVO/SEM SENHA), toggle "Mostrar inativos" (default só ativos). **38 tests** em `test_usuarios.py`. Decorator `'usuarios': ['1','6']` + `'configuracoes': ['1','6']`. **Fix card Médio Comercial** (2026-05-18): sparkline em `grid-row: 2 / 4` (span 2 rows) — antes ocupava só row 2 e a altura natural de 135px fazia row inteira esticar, desalinhando Médio em row 3. |
| **Refator oracle_conn.py revertido** (sessão Mai/2026, 2026-05-17) | ⚠ Revertido | Tentativa de dividir `oracle_conn.py` (10k linhas) em 16 sub-módulos via barrel pattern. Mecanicamente funcionou (manage.py check OK), mas **133 tests quebraram** por `@patch('oracle_conn.X')` não alcançar referências importadas localmente nos submódulos. Revertido com `git checkout` + apagar pasta `oracle/`. Pendente retomar com **caminho A** (atualizar todos os patches dos tests). Mapping das 116 funções já feito, ver memory `refator_oracle_views_pendente`. |
| **Sweep mobile v2 — iPhone Safari (v1.1.2)** (sessão Mai/2026, 2026-05-15) | ✅ Concluído · ⚠ Em testes em produção | **Causa raiz identificada**: 2 pegadinhas combinadas no iOS Safari: (1) `100vh` NÃO desconta a barra inferior do navegador → footer dos modais escondido; (2) `env(safe-area-inset-bottom)` retorna 0 sem `viewport-fit=cover` no `<meta viewport>`. **Fixes em 9 arquivos**: (a) `base.html` + `home_login.html` → `viewport-fit=cover`; (b) `global.css` → `flex-shrink: 0` em `.modal-header/footer/.cb-modal-header/footer`, `100dvh` com fallback `100vh`, buffer `90px+env` no `.main-layout` em ≤900px (`100px+env` em ≤520px); (c) `entrada.css` → modais dock (`#cabCard`/`#cabItemsCard`/`#rodapeCard`) viram fullscreen empilhado em ≤900px com z-index natural cobrindo; (d) `classificacao.css` → aside 32vh (era 38), LOTES `min-height: 60vh`, padding-bottom safe-area; (e) `comercial.css` → `.layout` rola vertical com `min-height: 320px` por card; (f) `venda.css` → padding-bottom safe-area no `.venda-grid`; (g) `rastreio.css` → painéis `minmax(70vh, auto)` (era 40vh fixo); (h) `combustivel.css` → `.cb-col-estoque` + `.cb-col-requisicoes` ambos `min-height: 75vh`, modais `100dvh`, footer com `padding-bottom: env(safe-area-inset-bottom)`. **Bug latente corrigido**: regra anterior usava `.cb-col-mov` (classe inexistente) → card Movimentações colapsava. Trocado pra `.cb-col-requisicoes` (classe real do template). Cache busting bumped em 7 templates. Validação: `manage.py check` ok, 262 tests passam. |
| **Balanço físico Combustível — ajuste SALDO_INICIAL_TANQUE (v1.1.2)** (sessão Mai/2026, 2026-05-15) | ✅ Concluído | Após balanço físico nos tanques, ajustada constante `SALDO_INICIAL_TANQUE` em [`oracle_conn.py:5898-5902`](../sankhya_integration/services/oracle_conn.py#L5898-L5902): Diesel S10 (392) `300.0 → 896.0` (+596 LT pra bater 3000 LT físico); Diesel S500 (1373) `3150.0 → 3204.0` (+54 LT pra bater 2490 LT físico); ARLA 32 (1374) inalterado. **Sem escrita no Oracle** — só constante Python (carregada no boot do processo Django). Sintoma: card Estoque passa a refletir saldo físico real. **Restart do serviço Windows IAgro necessário** após deploy (`nssm.exe restart IAgro`). 3 tests refatorados em `test_views_combustivel.py` pra ler `SALDO_INICIAL_TANQUE` via import em vez de hardcodar — não quebra mais em ajustes futuros. Sincronizado em `schema.md` e `dependencias_sankhya.md`. |
| **Módulo Relatórios — MVP (5 relatórios)** (sessão Mai/2026, 2026-05-17) | ✅ Concluído · ⚠ Em testes em produção | **Módulo novo `/sankhya/relatorios/`** com 5 relatórios em sub-abas (lazy load, 1 fetch por aba ao ativar). Acesso `1, 6, 9` (Diretoria/Suporte/Comercial). **Backend Cat A**: 5 funções aditivas em `oracle_conn.py` — `consultar_top_clientes_produtos` (dedup matriz, métricas valor/qtd/pedidos), `consultar_lotes_envelhecidos` (view `ANDRE_IAGRO_SALDO_LOTE` + `TRUNC(SYSDATE-DTNEG_ORIGEM)`), `consultar_consumo_ranking_veiculos` (TGFVEI+AD_REQ_COMBUSTIVEL agregando litros/valor + cálculo km/L ou L/h via medidores), `consultar_fluxo_caixa` (TGFFIN em aberto agrupado por buckets ATRASADO/HOJE/1-7d/8-15d/16-30d/31-60d/61-90d com saldo acumulado), `consultar_margem_por_venda` (CTE `custos_lote` + LEFT JOIN — receita TOP 35/37 'L' × custo proporcional via TOP 13/11). **5 endpoints REST** sob `/sankhya/relatorios/api/`. **Cache Django** (`django.core.cache`, TTL 5min) na margem (query pesada com CTE) — chave `(date_de, date_ate, agrupar)`, invalidação via `?nocache=1`. **Frontend**: 1 template + 1 JS modular (RENDERERS por sub-aba), filtros padronizados (chips de período + selects), tabelas com bandeiras coloridas (margem +/−/BAIXA/PREJU, lotes ATENÇÃO/ALERTA/CRÍTICO). Responsivo: <520px só mostra ícones nas tabs. **Tests**: 56/56 em `test_relatorios.py` (acesso por grupo, validação datas, fallback Oracle, payload completo, agrupamento, cache hit/miss/invalidação). 196/196 OK nas suítes tocadas. Próximas iterações já mapeadas: 20 relatórios restantes do backlog em `roadmap.md` + export Excel/PDF (intencionalmente fora do MVP — esperando feedback do operador real). |
| **Comercial — card "Margem Lote" preenchido** (sessão Mai/2026, 2026-05-17) | ✅ Concluído · ⚠ Em testes em produção | Card que estava sempre "0%" agora calcula margem realizada/provisória em tempo real ao clicar num lote. **Fórmula**: `(RECEITA_BRUTA − DEVOLUÇÃO − CUSTO) / RECEITA_LIQ × 100` — devolução desconta receita; avaria não duplica (custo já está no vale TOP 13) mas vem **informativa** no tooltip como `qtd × custo_médio_kg`. **Backend Cat A**: nova função [`consultar_margem_do_lote(codagregacao)`](../sankhya_integration/services/oracle_conn.py) com 4 SELECTs (vendas dedup, devolução TOP 36, custo+qtd+avaria via CASE em 1 query, saldo da view ANDRE_IAGRO_SALDO_LOTE pra status PROVISORIA/FECHADA). Tolerância a falha da view (assume saldo=0 e segue). Novo endpoint `GET /sankhya/comercial/api/margem-lote/?lote=X` ([api_margem_lote](../sankhya_integration/views.py)). **Frontend**: chamada em paralelo (`Promise.allSettled`) com `carregarVendasNoModoAtual` — não bloqueia uma na outra. Cores via `data-margem-cor` no `#distMini1` (verde #16a34a + sinal "+" pra positiva, vermelho #dc2626 + "−" pra negativa, cinza neutra). Badge âmbar `⚠ provisória` quando lote ainda tem saldo > 0. Tooltip nativo (`title`) com detalhamento numérico linha-a-linha. Estado "—" quando lote selecionado sem vale ainda (tem_custo=False) — evita mostrar "100%" enganoso. Cache: `comercial.css?v=6`, `comercialDistribuicao.js?v=4`. **Tests**: 13/13 em `test_margem_lote.py` (positiva/negativa/zero, divisão por zero sem explodir, devolução desconta receita, avaria informativa sem duplicar, PROVISORIA vs FECHADA, tem_custo=False, falha Oracle, falha da view com fallback). 140/140 OK nas suítes tocadas sem regressão. |
| **Comercial — últimas vendas DO LOTE + sparkline de preço** (sessão Mai/2026, 2026-05-16) | ✅ Concluído · ⚠ Em testes em produção | **Coração da operação comercial**: lista lateral "Últimas Vendas" passou a filtrar pelo **lote selecionado** (`CODAGREGACAO`) em vez de "produtos que existem no lote" — pra o time saber o histórico real de saídas daquele lote específico e negociar com base nele. Função antiga `consultar_lista_ultimas_vendas` preservada por retrocompat; nova `consultar_vendas_do_lote(codagregacao)` em [oracle_conn.py](../sankhya_integration/services/oracle_conn.py) com **dedup pedido↔nota** (preferência TOP 34 verdade IAgro; TOP 35/37 só sem par TGFVAR — mesma regra de `baixas_venda` da view `ANDRE_IAGRO_SALDO_LOTE`). Novo endpoint `GET /sankhya/comercial/api/vendas-lote/?lote=X` ([api_vendas_do_lote](../sankhya_integration/views.py)). Label "Últimas Vendas" → "Últimas vendas deste lote". **Sparkline SVG inline** (card `#cardSparkVendas` full-width no fim do `#distGrid`, antes dos botões): linha verde Agromil ligando preço/kg cronológico (esq=antigo, dir=recente), 3 linhas de grade (min, média tracejada, max), pontos hover-ativos com tooltip (cliente · data · tipo · preço), stats no header (média/min/max/nº vendas). **1 fetch único** alimenta lista + sparkline (sem dobrar query). Sem dependência externa — consistente com tanques combustível e gauge comercial. Limpeza no `limpar()` do `comercialDistribuicao.js` cobre troca de lote. **Tests**: 10/10 em [test_vendas_lote.py](../sankhya_integration/tests/test_vendas_lote.py) (SQL com filtros corretos + dedup TGFVAR + mapping SELECIONADO→tipo + cálculo preço/kg+cx + truncamento cliente em 24 chars + falha Oracle tolerada + view 400 sem lote + delega ao service). Cache: `comercial.css?v=4`, `comercialDistribuicao.js?v=2`. |
| **Rastreio etiqueta — peso da TOP 26 + opcional no vínculo + modal de escolha** (sessão Mai/2026, 2026-05-16) | ✅ Concluído · ⚠ Em testes em produção | **Coerência semântica**: Rastreio passa a usar `TGFITE.PESO` (campo canônico do peso/caixa em todas as TOPs IAgro) em vez de `QTDFIXADA` na TOP 34/35/37/30/36. `QTDFIXADA` na TOP 34/35 era populada com zeros default pelo Sankhya nativo (~99.999% de 380k linhas) — IAgro para de poluir. **Backend Cat B**: `atribuir_lote_item_pedido` ganhou parâmetro `peso` (renomeado de `qtdfixada`) e UPDATE/INSERT grava em `TGFITE.PESO` em vez de `QTDFIXADA`. `desvincular_lote_item_pedido` limpa `PESO` no caminho CLEAR. **Backend Cat A** (3 funções): (a) nova `consultar_pesos_classificacao_lote(codagregacao)` lê `SELECT DISTINCT PESO` da TOP 26 do mesmo lote (filtra STATUSNOTA<>'E' + PESO>0, ordem DESC); (b) `consultar_dados_etiqueta_pedido` refatorado pra retornar uma linha por SEQ TGFITE (sem agregação por CODPROD), com `peso_resolvido` em cascata: override do operador → `TGFITE.PESO` próprio → DISTINCT da TOP 26 (1 único = automático, 2+ = `precisa_escolha=True`). Aceita `pesos_overrides={seq: peso}` do modal. Cacheia pesos da TOP 26 por lote (evita N queries pra mesmo CODAGREGACAO em pedido multi-SEQ). (c) **Novo endpoint** `GET /api/resolver-peso/?nunota=X[&codprod=Y]` — frontend chama antes do PDF pra decidir entre abrir direto ou mostrar modal. (d) `api_rastreio_etiqueta_pdf` aceita `?pesos=seq:val,...` e retorna 409 + `itens_pendentes` quando alguma linha precisa de escolha sem override. **Frontend**: campo "Peso da caixa (kg)" no modal de vínculo virou **opcional** (asterisco removido, placeholder "opcional — usa o da classificação"). Validação JS só bloqueia formato inválido (vazio passa). Novo modal `#escolhaPesoOverlay` com radios por SEQ ambígua. Função única `_abrirPdfEtiquetas(nunota, codprod)` substitui os 3 `window.open` antigos — chama `resolver-peso` → abre PDF direto ou modal de escolha → re-abre PDF com `?pesos=...`. Cache `?v=3` em `rastreio.css`, `?v=2` em `rastreio.js`. **Split por peso = N vínculos**: mesma mecânica do SPLIT por qtd. Operador faz 2 vínculos (500kg @ 22kg + 300kg @ 20kg) → 2 linhas TGFITE com PESO diferente → etiqueta fiel ao split (23 cópias de 22kg + 14 de 20kg). **Sem migração SQL necessária**: pedidos antigos com QTDFIXADA preenchida (~5 itens em todo o banco) caem automaticamente no fallback TOP 26. Volume real de PESO TOP 26 cobre o universo (6.640 itens com PESO > 0). **Tests**: 27/27 em `test_etiqueta_lote.py` refatorados (PESO em vez de QTDFIXADA + 3 tests novos pra `consultar_pesos_classificacao_lote` + 2 pro endpoint `resolver-peso` + cobertura precisa_escolha 409). 77/77 do rastreio sem regressão. |
| **Sweep mobile v3 — dock fix Entrada + helper double-tap + home KPIs** (sessão Mai/2026, 2026-05-15) | ✅ Concluído · ⚠ Em testes em produção | **Bug crítico Entrada**: os 3 modais (`#cabCard`/`#cabItemsCard`/`#rodapeCard`) usam sistema dock lado-a-lado em desktop com `style.left` calculado em JS. Em iPhone, `itemsCard` caía em `left ≈ cabRect.right+8` > 100vw → 80% fora da viewport. CSS `!important` não resolvia (JS sobrescreve em runtime). **Fix**: novo bloco `setupMobileDockEntrada()` no fim de `entrada.js` com `MutationObserver` observando `style` dos 3 cards — em mobile (`matchMedia ≤900px`) força fullscreen empilhado com z-index hierárquico (cab 72 < items 77 < rod 82). Sem `display:none` (preserva handler do duplo clique). Loop prevention com flag `_applying`. **Form interno do cabItemsCard**: 5 campos (Produto/Vol/Qtd/Peso/Total KG) que ficavam horizontais agora viram coluna em ≤900px via `flex-direction: column !important` + `width: 100%` em cada filho. **Bug iOS Safari/Chrome**: `dblclick` não dispara em touch (suprimido em favor de gesto de zoom). Mesmo em Chrome iOS (WKWebView) e DevTools mobile emulation. **Helper centralizado** [`IAgro.onDoubleActivate(el, handler, opts)`](../sankhya_integration/static/sankhya_integration/iagro_helpers.js) cria `dblclick` nativo + fallback `click+timer 350ms` quando device é touch-capable. Sem dedup: desktop puro só dispara nativo, touch só dispara fallback. Aplicado em 5 lugares: `entrada.js:178+1859`, `classificacao.js:896+993`, `venda.js:318`. **Bug home KPIs cortados**: `home.css:209` tem regra `body.app-shell .main-layout { padding: 0 }` que zerava o safe-area-inset-bottom global. Reaplicado em ≤900px (`90px+env`) e ≤520px (`100px+env`) + novo breakpoint 1 coluna em ≤520px. Cache bumps: `iagro_helpers.js?v=3`, `entrada.js?v=5`, `classificacao.js?v=1003`, `venda.js?v=2`, `entrada.css?v=10`, `home.css?v=4`. |
| **Phosphor Icons + UI polish sidebar/vendas** (sessão Mai/2026, 2026-05-14) | ✅ Concluído | **Iconografia uniforme**: substituídos todos os emojis Unicode + Material Symbols + SVGs inline de UI por **Phosphor Icons** (CDN unpkg, regular + fill). Sweep cobriu 9 templates HTML + 7 arquivos JS + global `IAgro.showToast`. Item ativo da sidebar usa variante `fill` via `font-family: 'Phosphor-Fill'`. Material Symbols totalmente removido (import órfão + regra CSS). SVGs decorativos preservados (tanques combustível, gauge comercial). Mapeamento sidebar: squares-four/shopping-cart-simple/arrows-split/crosshair/currency-circle-dollar/flow-arrow/tray-arrow-down/gas-pump/magnifying-glass. Brand "Gestão Integrada" → "Gestão Inteligente". Padronização documentada em [`conventions.md`](conventions.md) → "Ícones — Phosphor Icons". **Fix sidebar Comercial**: `.sidebar` sem escopo no comercial.css impedia recolhimento (200px fixo); escopado pra `.layout > .sidebar`. **Fix piscada da sidebar**: script inline pós-`<aside>` aplica `.collapsed` antes do paint + classe `preload` no body desativa transitions no carregamento (removida no `load`). Cache `?v=7` no global.css. **Renomeação dos grupos Sankhya**: TSIGRU CODGRUPO=8 `PACKING_ENTRADA` → `IAGRO_PACKING`; 9 `IAGRO_COMERCIAL`; 10 `PACKING_VENDAS` → `IAGRO_ADMINISTRATIVO`; 11 `PACKING_FROTA` → `IAGRO_FROTA`. Zero impacto funcional (código sempre usou CODGRUPO numérico). **Matriz de permissões revisada**: Rastreio perde COMERCIAL (`['1','6','8','9','10']` → `['1','6','8','10']` — comercial verá rastreio só via Relatórios); Combustível ganha ADMINISTRATIVO (`['1','6','11']` → `['1','6','10','11']`); nova chave `relatorios: ['1','6','9']` antecipando módulo futuro. 2 tests adaptados. **Tabela Pedidos da Venda redesenhada (Cat B aprovada)**: SELECT da `listar_vendas_paginado` ganhou `c.OBSERVACAO` (col 8); nova coluna **Observação** entre Parceiro e Total (180px, truncamento + tooltip); colunas redimensionadas (Pedido 70→90px, TOP 40→80px, Data 90→60px); Total reforçado com `text-right`. Labels TOP renomeados: `34 - PDV`, `35 - NFe`, `37 - S/NFe`, `36 - Dev`, `30 - AVA` (removido `text-transform: uppercase` global do badge pra preservar mixed-case "NFe"). Avaria 30 ganhou vermelho forte (`#fecaca/#7f1d1d/700`), devolução 36 mudou pra laranja (`#fed7aa/#9a3412`). Backlog do **módulo Relatórios** mapeado em 6 eixos com MVP de 5 itens. |
| **📱 Redesign Mobile app-like — Entrada (sessão Mai/2026, 2026-05-26/27)** | ✅ Concluído · ⚠ Em testes em produção | **Primeira implementação mobile-first** do IAgro com paridade exaustiva com a versão web. Estratégia: HTML único com 2 containers paralelos (`.entrada-desktop` + `.entrada-mobile`), escopados por `body[data-active-module="entrada"]` em viewport ≤900px. Desktop preservado 100% intacto. **Componentes**: 5 telas (`.m-screen` empilhadas, 1 ativa por vez) + 3 bottom sheets (Nova nota, Itens, Cabeçalho) + bottom nav 4 itens + 2 FABs (Nova nota verde + Atualizar azul). **Cards de notas em 1 linha** (26px altura — ~22 cards em viewport 720px). **Swipe gestures**: swipe-to-back nas telas internas + swipe-to-delete nas linhas (arrasta esquerda revela lixeira 56px). **Paridade desktop validada via levantamento exaustivo (14 itens)**: auto-cura cabeçalho órfão (`POST /nota/delete/` se fechar sheet sem add item), trava `apenas_checar` antes do confirm em delete (item E nota), editar item via tap (modo edit + botão "Salvar"), Vol != CX força peso=1 + blur restaura "CX", campos inválidos com `.is-invalid` vermelha, Vale lock 409 handling (`handleValeLockedError`), avaria fornecedor inline (auto-save no blur, só não-classificáveis), detecção de duplicação (CODPROD+CODVOL), Enter dispara Add em qtd/peso/produto, cabeçalho_excluido handling, editar cabeçalho existente via `?ajax_header=1`. **Estados**: `ESTADO_NOTA` (nota da tela 2) + `ESTADO_ITENS` (sheet aberto + `cabRecemCriado`). **Tela 3 "Salvar e próximo"** navega automático pro próximo pendente; quando todos conferidos volta pra tela 2 com toast "Nota completa ✓". **Limpeza arquitetural junto**: removidas referências mortas à página `/sankhya/compras/central/` (removida no início do projeto). View `view_central_compras` agora retorna 410 Gone (mantém só ramo `?ajax_header=1`). 29 tests OK, zero regressão. Cache CSS `?v=29`, JS `?v=14`. |
| **📱 Redesign Mobile app-like — Classificação (sessão Mai/2026, 2026-05-26)** | ✅ Concluído · ⚠ Em testes em produção | Segundo módulo com mobile. Mesma arquitetura da Entrada (escopo `body[data-active-module="classificacao"]`). 2 telas + 2 bottom sheets (Filtros + Descarte). **Cards de lote com cor de status**: 🟢 Verde (Finalizada) · 🟡 Âmbar (Classificando) · 🔴 Vermelho (A Classificar). **Tela detalhe** com hero (Fornecedor/Produto/Pedido) + grid 4 cards de resumo (In natura · Classificado · Descarte · Estoque com kg e %) + toggle "Classificação Finalizada" (POST `/sankhya/item/toggle_status/`) + lista de classificados read-only + FAB que redireciona pro editor desktop. **Bottom sheet Descarte** com `+/−` (toggle Adicionar/Subtrair) → POST `/sankhya/item/update_descarte_lote/`. **Filtros**: status chips coloridos + Data ini/fim + `<<` `>>` + Pedido + Produto (fabricante) + Parceiro + Lote — Aplicar copia pro form desktop + recarrega `/api/lotes/`. **Decisão pragmática**: Adicionar nova classificação e editar item classificado **redirecionam pro editor desktop** com `?open=items&sel=LOTE` — fluxo `modalClassify` (Origem + Planificar + SQL preview) complexo demais pra reimplementar em mobile. Visualização entrega ~80% do valor (operador confere status, lança descarte, fecha classificação no celular). 8 tests audit OK. Cache CSS `?v=3`, JS `?v=1`. |
| **UX polish + integração home/vendas/email** (sessão Mai/2026, 2026-05-08) | ✅ Concluído | **Rastreio (10 melhorias UX em homologação)**: skeleton/spinner durante fetch inicial das duas colunas (substitui "Carregando..." pelado), `.btn--loading` com spinner CSS em ações de atribuir/desvincular pra prevenir double-click, toast contextual (`Lote 8117 vinculado: 160 kg → Pedido 4567 · CLIENTE X`), `ORA-00054` reescrita pra "Outro operador está mexendo neste registro agora. Aguarde alguns segundos…", erro de saldo insuficiente em formato BR + sugestão de ação (`Disponível: 125,50 · Solicitado: 200,00. Reduza a quantidade ou desvincule…`), empty states com botão `Limpar filtros` / `Mostrar pedidos vinculados`, mensagem distinguindo filtros restritivos de "realmente sem dados", tooltips em badges técnicos (`N/C`, `AVARIA`, `FATURADO`, `ATRIBUÍDO` com TOP-nome), microcopy `Confirmar` → `Vincular lote`. Tests `test_rastreio.py` + `test_views_venda.py` ajustados pros novos termos da humanização. **Vendas + Importação e-mail**: novo `btnImportarPedidos` na toolbar de Vendas (ícone download/inbox, ao lado da impressora) abre `/sankhya/venda/email-importar/` em **aba nova** via `window.open(url, '_blank')` com polling `aba.closed` que dispara `carregarVendas(false)` ao fechar — reflete pedidos confirmados durante a sessão. **Home**: novo card `📥 Importação` depois do Rastreio. **Tela de revisão de e-mail reorganizada**: 7→6 colunas no `<table>` de itens — `Texto do PDF` agora carrega `cod_cliente - descricao` combinado (ex: `1042608 - MILHO VERDE C/5UN`); colunas `Cód cliente` e `Vol` removidas; nova célula unificada `Qtd+Vol` (`160 KG`, vol em fonte 11px dentro de `.qtdvol-wrap`); nova coluna `Total` com `qtd × preço` por linha (recalcula reativo em `atualizarTotais`); painéis ajustados (Fila 320→260px, split PDF/items 1fr/1.35fr); padding reduzido pra 5px 6px. Pill do match (`alias`/`~XX%`/`✓`) reposicionado de `top: -6px` (saía pra fora) para `bottom: 1px` (canto inferior direito **dentro** da célula). Lógica do badge **mantida**: `✓` = você editou nesta sessão (CODPROD_FINAL preenchido), `alias` = aprendizado prévio aplicado, `~XX%` = fuzzy. Validação: `manage.py check` ok, 174 testes (Rastreio + Venda + Comercial + Entrada) passam. **Fix do typeahead de produto na revisão de e-mail (mesmo dia)**: 2 ajustes — (a) `attachTA` ganhou suporte a `extraQuery` e os 2 typeaheads de produto (linha por linha + modal "Adicionar item manual") agora passam `grupo_inicia_com=1` (mesma regra da Venda — só hortifrúti vendável); (b) dropdown de typeahead dentro de `<td>` em tabela com `border-collapse: collapse` não aparecia mesmo com `display: block` — corrigido movendo o `.dropdown-abs` pro `<body>` antes do show e usando `position: fixed` com coordenadas via `getBoundingClientRect()` do input. Vide gotchas.md ("Dropdown de typeahead dentro de <td>"). **Bloco A de UX no Rastreio (mesmo dia, pós-feedback)**: 5 quick wins puramente frontend — (1) avaria interna como linha B (`.nao-vendavel` separada com tag `AVARIA INT.`, badge inline descontinuado); (2) métrica `kg a atribuir` (vermelho) na quickstats de pedidos pra confrontar com `kg disponíveis` (verde) dos lotes; (3) badge `✨ encaixa` em lote cujo saldo bate exatamente com a falta total daquele produto + borda esquerda verde; (4) atalhos globais de teclado (`/`-`F` foca busca, `R` Atualizar, `C` Limpar, `G` alterna agrupamento parceiro↔produto), Esc já existia; (5) persistência do lote armado em `localStorage` (chave `iagro:rastreio:prefs:v1` campo `loteArmadoCodag`), restaurado 1× por boot após `carregarLotes`. Helpers novos: `_calcularFaltaPorCodprod`, `_criarCardLoteAvariaInterna`, `_tentarRestaurarLoteArmado`. CSS novo: `.tag-avaria-int`, `.col-qtd-avaria`, `.ras-row-check-box-disabled`, `.badge-encaixa-exato`, `.lote-encaixa-exato`. Validação: `manage.py check` ok, 140 testes (Rastreio + Venda) passam. |

---

## Módulos em andamento

Parcialmente implementados ou que dependem de decisão externa.

| Item | O que existe | O que falta | Próximo passo |
|---|---|---|---|
| **Controle de Combustível** (Mai/2026, atualizado 2026-05-13) | ✅ **Ponta-a-ponta funcional em produção com CRUD completo**. Sprint Mai/2026 ampliou muito o escopo original. **Backend**: ~14 funções service (leitura: saldo, veículos, produtos, prazo TGFTPV, último preço, consumo por veículo, listagens, obter requisição/entrada; escrita: B2 criar_req, B5/B11 editar_req, B6/B12 excluir_req, B8 criar_externo, B13 criar_entrada, B14 editar_entrada, B15 excluir_entrada). 14 views Django + 14 rotas. DDLs: `AD_REQUISICAO_COMBUSTIVEL` (B1) com `HODOMETRO_KM`+`HORIMETRO_H` (B4) + colunas `CATEGORIA`/`CODPARC`/`NUFIN_GERADO` (B7) pra suportar EXTERNA_POSTO; view `ANDRE_IAGRO_SALDO_COMBUSTIVEL` filtra TOP 53 + `NOT EXISTS EXTERNA_POSTO`. **TOP 26 → TOP 53** em todo o módulo (TOP 26 reservada à Classificação). **Multi-itens** em entrada (B13) e em abastecimento externo (B8 + B11 caminho externo): payload `itens=[{codprod,qtd,vlrunit},...]` com compat retroativa de codprod/qtd/vlrunit avulsos. **CRUD completo de entrada** (criar/editar/excluir tudo via IAgro — Sankhya não precisa). **Frontend**: tanques SVG, lista de veículos com toggle COM/MAQ + foto thumb cacheada via Pillow (endpoint `/veiculo-foto/?size=thumb` em `_cache/`) + lightbox full-screen. Modal Nova Entrada com tabela dinâmica multi-itens + Total da Nota reativo + NºNota/Série + auto-cálculo DTVENC (BASEPRAZO/regex DESCR). Modal Nova Requisição alterna entre form single (interno) e tabela multi-itens (externo). Auto-fill VLRUNIT do último abastecimento (readonly em internos, editável em externos). Listagem 9 colunas (removidas NUNOTA+Status, adicionadas Total km + Média calc client-side). Badge `🌐 EXTERNA` laranja distinto. Exclusão de entrada migrou pra dentro do modal de edição (lixeira na linha removida pra evitar deletar nota multi-itens por engano). **TGFFIN sempre nasce em aberto** (DHBAIXA=NULL, VLRBAIXA=0, ORIGEM='E', CODTIPOPERBAIXA=0) — 3 triggers Sankhya bloqueiam baixa automática (TRG_UPT_TGFFIN_NUBCO + TRG_INC_TGFFIN). Humanizar ORA-20101 extrai msg real do trigger via regex. `gerar_proxima_sequencia_item` aceita `conexao_existente` (evita ORA-00001 em multi-INSERT transacional). **84 tests** mockados (313 totais cruzando módulos). | **Cadastro humano no Sankhya pendente**: (1) atribuir usuários ao grupo `TSIGRU.CODGRUPO=11` (IAGRO_FROTA — renomeado de PACKING_FROTA em 2026-05-14); (2) máquinas/tratores em TGFVEI (PROPRIO='S'); (3) freteiros em TGFVEI (PROPRIO='N'). Tudo o resto (DDLs, view, código backend/frontend, tests, smokes reais) deployado e validado. | Operador cadastra (1)(2)(3) → smoke real em produção com requisições e abastecimentos externos |
| **Emissão real de NFe (TOP 35)** | Pedido marcado faturado em TGFCAB com `STATUSNOTA='L'`, NUMNOTA gerado | Disparo real da emissão via webservice/API do Sankhya | **Decisão de negócio:** (a) automático após faturar, (b) manual no Sankhya (estado atual), (c) job batch periódico |
| **Vínculo lote ↔ item dentro do modal de Venda** | Item da Venda com `CODAGREGACAO=NULL`; vínculo no Rastreio | Typeahead dinâmico no modal de itens da Venda filtrando por saldo de lote | **Recomendação:** manter fluxo atual — Venda → Rastreio é suficiente |
| **Avaria interna como linha separada (Opção B)** | ✅ Concluído em Mai/2026 — card `.nao-vendavel` com tag `AVARIA INT.` logo abaixo do vendável (commit f8486b5) | — | — |
| **Rastreabilidade no pedido + listagem unificada** (Mai/2026, 2026-05-11) | ✅ Concluído — rastreabilidade vive sempre no TGFITE do pedido (TOP 34), mesmo após faturamento Sankhya. Suporta SPLIT em pedido faturado. View `ANDRE_IAGRO_SALDO_LOTE` baixa de TOP 34 STATUSNOTA='L' com prioridade sobre TOP 35/37 (deduplicação via TGFVAR). Toggle Pendente/Faturado mostra TOP 34; badge laranja `FATURADO Nota Y` em cards faturados via TGFVAR. **Fix NUMNOTA** (bug onde notas faturadas mostravam NUNOTA interno) — backend agora retorna `nota_numnota` e `nota_nunota` via subquery TGFVAR. Tela Rastreio também ganhou: busca de fornecedor lado a lado com busca lote, remoção do botão Travar, listagem dedup pra não mostrar pedido+nota como duplicata. Aceitar STATUSNOTA='L' em `atribuir_lote_item_pedido` e `desvincular_lote_item_pedido`. | Em testes em produção | — |
| **Nota órfã (TGFVAR vazio): vincular ou criar pedido retroativo** (Mai/2026, 2026-05-11) | ✅ Concluído. Notas TOP 35/37 STATUSNOTA='L' sem TGFVAR par viram **NOTA_ORFA** (badge vermelho `ÓRFÃ`) na listagem do toggle Faturado. Tabela auxiliar **`AD_VINCULO_PEDIDO_NOTA`** (DDL versionada) registra vínculos manuais com `ORIGEM='VINCULADO'` (Leva A — pedido pré-existente) ou `ORIGEM='PEDIDO_RETROATIVO'` (Leva B — IAgro cria TGFCAB TOP 34 espelhando a nota). Fluxo unificado num **botão único adaptativo** no card: backend decide via heurística rigorosa (mesmo CODPARC+CODEMP + valor exato ∆≤R$0,01 + data ±1 dia). Endpoint `/api/vinculo/resolver/` chama internamente `inserir_vinculo_manual_pedido_nota` (Leva A) ou `criar_pedido_retroativo_a_partir_de_nota` (Leva B). Operador sempre confirma antes via modal. Reversível pelo botão "Desfazer" no badge; em Leva B, pedido criado também é DELETE (só se nenhum lote foi atribuído ainda). Casos reais validados contra Oracle: Nota 6267 (NUNOTA 111976) → vincula com Pedido 111975 (R$ 4.200 exato); Nota 6242 (NUNOTA 111825) → cria pedido retroativo. Refinamento de heurística: primeira versão usava janela ±7 dias sem filtro de valor, gerando falsos positivos pra clientes recorrentes (Assaí faz pedidos quase todo dia). | Em testes em produção | — |
| **`AD_NUMPEDIDOORIG` na nota** (Mai/2026, 2026-05-11) | ✅ Concluído. Convenção Agromil "venda aponta pro pedido origem via AD_NUMPEDIDOORIG" agora aplica também nos cenários invertidos. Ao **vincular** ou **criar pedido retroativo**, IAgro faz UPDATE em `TGFCAB.AD_NUMPEDIDOORIG = NUNOTA_pedido` + `TGFITE.AD_NUMPEDIDOORIG = NUNOTA_pedido` (todos os itens da nota). No **desfazer**, volta pra NULL. Tudo atômico no mesmo commit do INSERT/DELETE em AD_VINCULO_PEDIDO_NOTA. Pedido criado retroativamente recebe `AD_NUMPEDIDOORIG = próprio NUNOTA` (auto-referência, default da `inserir_cabecalho_nota_banco`). | Acompanhar em produção | — |
| **Busca de fornecedor real (NOMEPARC_ORIGEM)** (Mai/2026, 2026-05-11) | ✅ Concluído. Bug encontrado: typeahead de "Buscar fornecedor" filtrava por `TGFPRO.FABRICANTE`, mas na Agromil essa coluna está populada com nome de produto (LIMÃO, BATATA DOCE…) — não com fornecedor de fato. Refatorado: `consultar_fabricantes_disponiveis` agora consulta DISTINCT em `NOMEPARC_ORIGEM` (parceiro da TOP 11 do lote) — fornecedor real (DEBORA, BRUNO, JOSE DO ALHO…). Filtros do saldo e cross-filter de pedidos também migraram. Nome de função/parâmetro `fabricante` preservado por retrocompat. **Busca lote OU produto** combinada: input principal agora envia `q_lote_prod` que faz `UPPER(CODAGREGACAO) LIKE :q OR UPPER(DESCRPROD) LIKE :q`. | — | — |
| **Avaria (TOP 30) + Devolução (TOP 36) + Histórico do Lote** (Mai/2026, 2026-05-11) | ✅ Concluído. **3 botões novos na toolbar da Venda**: `⚠ Avaria`, `📤 Devolução`, `🔍 Histórico do lote`. **Avaria TOP 30** (CODNAT=20010200): cria TGFCAB+TGFITE STATUSNOTA='L' direto com `CODAGREGACAO` obrigatório (rastreabilidade por lote — Sankhya nativo deixa NULL). Saldo desconta automaticamente via view. **Devolução TOP 36** (CODNAT=10020100): cria STATUSNOTA='A' em aberto + TGFITE par-a-par preservando lote da nota origem + **INSERT em TGFVAR** replicando fielmente o Sankhya nativo (descoberta: 12 de 13 TOP 36 em produção têm TGFVAR populada já no INSERT). Operador confirma no Sankhya, que dispara financeiro reverso + NFe devolução. Trava anti-devolução-excessiva via `consultar_devolucoes_anteriores_de_nota`. **Histórico do Lote**: GET endpoint `/venda/api/lote/historico/` lê TGFITE+TGFCAB+TGFPAR de um CODAGREGACAO, devolve timeline ordenada por DTNEG ASC (compra → classificação → vale → pedido → venda → devolução/avaria). Frontend renderiza em modal com nós coloridos. **View `ANDRE_IAGRO_SALDO_LOTE` ganhou perna F (DEVOLVIDO)** e fórmula `QTD_DISPONIVEL` ganhou `+ QTD_DEVOLVIDA`. Validado em produção: lote 111752S01D260507 com 3.3 kg de devolução já reflete saldo correto (`ENTRADA=210, BAIXADA_VENDA=150, DEVOLVIDA=3.3 → DISPONIVEL=63.3`). **45 testes novos** (174 → 229 mantendo cobertura). Listagem da Venda ampliada pra `CODTIPOPER IN (30, 34, 35, 36, 37)` com badges coloridos por TOP. | Em testes em produção | — |
| **Padronização UX — typeaheads + auto-select + debounce + filtros** (Mai/2026, 2026-05-12) | ✅ Concluído. **3 helpers centrais em `iagro_helpers.js`**: `IAgro.attachTypeahead(opts)` substitui 3 implementações locais de `attachTA` (entrada/venda/email) + handlers ad-hoc em classificação/comercial — garante ↑/↓/Enter/Tab/Esc, blur tolerante, position:fixed pra dropdown em `<td>`, debounce 300ms padrão, `pickExtra` pra `data-*` customizados. `IAgro.installAutoSelect()` ativado uma vez em `base.html` faz delegação global de select-on-focus (opt-out: `data-no-select`). `IAgro.wireFilterAuto(ids, onChange, opts)` padroniza listeners de filtro (texto/número → debounce 500ms, select/date → change imediato). **Bug crítico corrigido**: typeahead não duplica código quando `cod===descr` (caso fabricante: antes mostrava `"CENOURA — CENOURA"`, agora `"CENOURA"`). **Bug retroativo**: `criarAutocomplete` da Classificação removia prefixo `"cod — "` do `exibicao` legado pra evitar `"601 — 601 — RD ABACATE"`. Retrofit de 7 módulos JS, ~588 inserções/-545 deleções. | Em testes em produção | — |
| **Fix Entrada — filtro lateral por fabricante alinha com Comercial** (Mai/2026, 2026-05-12) | ✅ Concluído. Bug histórico: o frontend mandava `codprod` mas o backend `listar_notas_compra_paginado` **ignorava silenciosamente** — operador filtrava e a lista nunca mudava. Investigação revelou desencontro arquitetural: frontend chamava endpoint de produtos individuais sem `&fabricante=1`. Refatorado pra Padrão A (Fabricante): URL `?fabricante=1`, typeahead renderiza só nome do fabricante (sem código), hidden recebe texto puro. Backend ganhou filtro `EXISTS (SELECT 1 FROM TGFITE i2 JOIN TGFPRO pr2 ON pr2.CODPROD=i2.CODPROD WHERE i2.NUNOTA=c.NUNOTA AND UPPER(pr2.FABRICANTE) LIKE %fab%)`. `views.py` passa `fabricante` como string em vez de `codprod` int. `prod_display` substituído por `params.fabricante` direto. Smoke validou: SEM filtro = 50 notas, COM CENOURA = 50 notas diferentes, 48 das 50 originais excluídas. | Em testes em produção | — |
| **Chips de filtros ativos em Entrada e Classificação** (Mai/2026, 2026-05-12) | ✅ Concluído. Padronização com Venda: classes `.iagro-filtros-ativos`, `.iagro-filtro-chip` etc movidas pra `global.css` (60 linhas); Venda mantém aliases `.vda-*` pra retrocompat. **Entrada**: 4 chips (Período/Pedido/Produto/Parceiro), click no × zera campo + `applyFilters()` (submit + reload). **Classificação**: 5 chips (Período/Pedido/Produto/Parceiro/Lote), click no × zera + `carregarTabelaLotes()` (AJAX). Hook wrappa `carregarTabelaLotes` original pra renderizar chips após cada AJAX. Período "hoje" não vira chip (evita ruído). | Em testes em produção | — |
| **Classificação — Nº Vale + Lote no fim + larguras** (Mai/2026, 2026-05-12) | ✅ Concluído. Tabela LOTES ganhou coluna **Nº Vale** (NUNOTA da TOP 11) entre Status e Data. Coluna **Lote** movida pra última posição alinhada à direita. Lazy load da paginação infinita atualizado pra bater com o render principal (estava com 8 colunas + ordem antiga, agora 9 colunas idênticas). Refatorado `cells[1]` (era índice da coluna Lote) → `cells[cells.length-1]` em 4 lugares pra robustez. Tabela "Produtos Classificados" reajustada com larguras proporcionais — coluna Produto recebe espaço expansível, Seq enxuto a 45px centralizado (antes truncava "GOIABA CORTIBEL EX..."). | Em testes em produção | — |
| **Docs: 2 padrões do campo "Produto" + regra #11** (Mai/2026, 2026-05-12) | ✅ Concluído. **`conventions.md`**: nova seção "Campo 'Produto' — dois padrões de filtragem" mapeando Padrão A (filtro por FABRICANTE, LIKE em `pr.FABRICANTE`) usado em filtros laterais (Entrada/Comercial) e Padrão B (filtro por PRODUTO específico, CODPROD int) usado em modais de item (Venda/Classificação modal). Inclui exemplos de código copiáveis (frontend + template + views.py + oracle_conn.py) e links pros arquivos reais. **`rules.md` regra #11**: antes de criar/alterar QUALQUER campo "Produto", PARAR e PERGUNTAR ao usuário qual padrão usar. Mesmo nome de campo, comportamentos diferentes — escolher errado causa filtro silenciosamente inútil. | — | — |
| **Cobertura services do Rastreio** | Indireta via mocks (53 testes) + diretos para `atribuir_lote_item_pedido` e `faturar_pedido_venda_banco` | Testes diretos de `consultar_vinculos_de_lote`, `consultar_fabricantes_disponiveis`, `desvincular_lote_item_pedido`, `consultar_saldo_lote_disponivel` | Replicar padrão do `AtribuirLoteServiceTest` |
| **WhiteNoise / `DEBUG=False` em produção** | Versão 1.2.0, healthcheck profundo, paleta visual fechada | Validar `collectstatic`, ativar WhiteNoise em `MIDDLEWARE`, configurar `STATIC_ROOT` | Subir em homologação com `DEBUG=False` e validar JS/CSS |
| **Typeahead de Lote no portal Venda** | Refino cosmético aplicado (placeholder "Código do lote" + title explicativo). Busca livre por `LIKE` com debounce 500ms | Typeahead real exigiria nova função em `oracle_conn.py` (`SELECT DISTINCT CODAGREGACAO FROM TGFITE WHERE...`) — bloqueado pela **regra crítica #4** | Decisão de negócio: vale o custo? `SELECT DISTINCT` em tabela grande sem índice dedicado pode ser caro |
| **Dashboard Executivo + Layout v2 (sidebar + header)** (Mai/2026) | ✅ Concluído | Reestruturação completa de `base.html`: sidebar lateral (200px expand / 56px collapse + tooltip + off-canvas em mobile) + content-header com user-badge/sair + footer inline. `home.html` virou dashboard com 6 indicadores: pedidos sem lote (Rastreio), lotes aguardando classificação (Classif), vales abertos (Comercial), tanques críticos <20% (Combustível), pedidos prontos pra faturar (Venda), lotes >60d (Rastreio). Indicadores em widgets coloridos (semáforo verde/amarelo/vermelho) com polling 5min + refresh manual. Tela de login standalone (`home_login.html`) com gradiente. Itens "Auditoria" só pra grupos 1/6. Toggle persiste em `localStorage`. 8 testes novos (`test_views_dashboard.py`). `consultar_indicadores_dashboard()` em `oracle_conn.py` consolida 5 SELECTs com tolerância individual + 1 chamada a `consultar_saldo_combustivel()`. `TANQUE_CRITICO_PCT=20.0` + `DIAS_LOTE_ENVELHECIDO=60`. |
| **Auditoria Universal — AD_AUDITORIA_GERAL + Tela** (Mai/2026, 2026-05-13) | ✅ Código concluído · ⚠ DDL precisa ser aplicada no Oracle | **Lote B1**: DDL `AD_AUDITORIA_GERAL` (ID, MODULO check 7 valores, OPERACAO, TABELA_ALVO, REGISTRO_ID, CODUSU, NOMEUSU, DT, SNAPSHOT_ANTES CLOB, SNAPSHOT_DEPOIS CLOB, OBSERVACAO) + Sequence + 4 índices (DT desc, CODUSU, MODULO, REGISTRO_ID) + helper `registrar_auditoria()` em `oracle_conn.py` (tolerante a falha, conexão própria pós-commit). **Lotes B2-B6**: 36 funções de escrita instrumentadas em Venda (9), Combustível (7), Rastreio (6 — gravação dupla com `RastreioAudit` SQLite legado), Comercial (6), Entrada/Classificação (8). **Lote A**: tela `/sankhya/auditoria/` com sidebar de filtros (módulo, operação, usuário, datas, busca livre, registro) + timeline paginada (50 por vez, ordem `DT DESC, ID DESC`) + modal com **diff inteligente em tabela amigável** ("antes → depois" em verde/vermelho com badge alterado/adicionado/removido) + JSON técnico expansível. Algoritmo: flatten + normalização só do último segmento + aliases (qtdneg/qtdvol → qtd, codvolparc → codvol). 37 testes novos cobrindo todos os lotes (B2-B6 + A). Acesso restrito a grupos 1 (Diretoria) e 6 (Suporte). |
| **Sweep responsivo p/ tablet em todos os módulos** (Mai/2026, 2026-05-13) | ✅ Concluído | Adicionadas seções `RESPONSIVO PARA TABLET / MOBILE` no fim de cada CSS de módulo (`combustivel.css`, `venda.css`, `comercial.css`, `rastreio.css`, `entrada.css`, `classificacao.css`, `email_importar.css`) com 3 breakpoints padronizados: `≤1024px` (compactação leve), `≤900px` (containers viram coluna única + tabelas com `overflow-x` + modais 95vw + botões hover-only viram sempre visíveis), `≤520px` (modais fullscreen, header compacto, grids 1 col). Defesa global em `global.css` pra modais sem regra específica (`max-width: min(640px, 95vw)`). Documentado em `conventions.md` → "Responsivo". Cache `?v=4` no `global.css`. |

---

## Pendências (ainda não iniciadas)

| # | Item | Esforço | Dependência |
|---|---|---|---|
| 1 | Manual operacional para usuários não-tech (fluxo de Venda + Rastreio com screenshots) | 1-2 dias | Tela final aprovada |
| 2 | Configuração formal de produção (`.env.dev` + `.env.prod`, nginx ou WhiteNoise, HTTPS, `SECURE_*`) | 1 dia | Servidor real disponível |
| 3 | Substituir `ControleInatividadeMiddleware` por timeout nativo (`SESSION_COOKIE_AGE` + `SESSION_SAVE_EVERY_REQUEST`) | 2 horas | — |
| 4 | Cobertura de testes da Classificação | 1 dia | — |
| 5 | Refator do CSS legado: separar `entrada.css` em `base-layout.css` (global) + `entrada.css` (específico) | 4 horas | Auditoria visual após split |
| 6 | Unificar `@keyframes` locais (`spin`, `iaspin`, `toastSlideIn/Out`) com nomes globais (`ia-girar`, `ia-toast-entrada/saida`) | 2 horas | Atualizar referências em JS |
| 7 | CSRF nas views POST com JSON: validar token sendo enviado corretamente pelo frontend | 2 horas | — |
| 8 | ✅ **E-mail: parser regex como Etapa 0 — IMPLEMENTADO Mai/2026** (entrada em "Módulos prontos para produção" como v5). Em testes em produção. | concluído | — |
| 9 | E-mail: refatorar `test_views_email_pedidos.py` (40+ testes usando schema antigo, falham hoje) | 4 horas | Estabilizar API após v3/v4/v5 |
| 10 | E-mail: agendar Task Scheduler do worker no servidor (`python manage.py colher_pedidos_email` a cada 30 min) | 30 min | Fechar primeira rodada de pedidos reais em produção |
| 11 | E-mail: adicionar `keep_alive=-1` na chamada Ollama pra modelo nunca descarregar da RAM (elimina latência da 1ª chamada do dia) | 15 min | — |
| 12 | E-mail: testar `qwen2.5:7b-instruct` lado-a-lado com 14B em 1 semana de uso real — se acurácia > 85%, virar default (3× mais rápido) | 1 semana de operação | — |
| 13 | **E-mail: avaliar migração LLM Ollama → Anthropic API** — análise completa de prós/cons em [`.claude/modules/email.md`](modules/email.md) → "Decisão pendente registrada". Custo estimado US$ 1,50–22,50/mês. Ganho: latência 30-60×, acurácia +5-15 p.p. **Bloqueado por LGPD (Anthropic não-cadastrada na ANPD) e por validar Etapa 0 em produção primeiro.** | 4-6 horas implementação + tempo indefinido de aval jurídico | (a) 1 semana da Etapa 0 em prod, (b) aval compliance/LGPD |
| 14 | ✅ **Rastreio: materializar saldo em `AD_SALDO_LOTE_CACHE` — IMPLEMENTADO Mai/2026 (2026-05-19)**. Tabela AD_* espelho da view `ANDRE_IAGRO_SALDO_LOTE` com refresh por Windows Task Scheduler a cada 5 min (`python manage.py refresh_saldo_lote`). Cache Django 60s + versionamento + invalidação automática em escritas (atribuir/desvincular). Endpoint manual `POST /api/refresh-saldo/` no botão Atualizar quando operador precisa de dado fresco imediato. Combinado com refactor da query de pedidos (WITH clause consolidando 5 subqueries correlacionadas em 1 LEFT JOIN), resultado em prod: lotes 12s → 80ms, pedidos 14.5s → 590ms. Operador sente tela completamente fluida. Detalhes em [`modules/rastreio.md`](modules/rastreio.md) + [`dependencias_sankhya.md`](dependencias_sankhya.md) §5.10. | concluído | — |
| 15 | **Rastreio: refresh on-demand (C2) pra eliminar defasagem de 5min** — atualmente após operador atribuir/desvincular lote, tela mostra estado antigo por até 5min (lock pessimista garante integridade). Próximo nível: MERGE pontual em `AD_SALDO_LOTE_CACHE` na linha afetada após escrita. Custo +500ms/escrita. Implementar **apenas se aparecer incidente real** após 2 semanas de uso (operador reclamando de defasagem). | 4-6h Cat B | 2 semanas de uso em prod sem incidente — provavelmente não precisa |

---

## Ordem de Prioridade Recomendada

Sequência otimizada para chegar à produção rápido sem dívidas críticas.

| Prioridade | Item | Justificativa |
|:---:|---|---|
| **P0** | Decisão de NFe (módulo em andamento #1) | Bloqueia produção |
| **P0** | Configuração formal de produção (pendência #2) | Bloqueia produção |
| **P0** | E-mail: agendar Task Scheduler do worker (pendência #10) | Operação real só começa quando worker roda automático |
| **P1** | E-mail: keep_alive Ollama (pendência #11) | Quick-win 15min, elimina latência da 1ª chamada |
| **P1** | Manual operacional (pendência #1) | Onboarding de operadores |
| **P1** | Cobertura services do Rastreio (em andamento #4) e Classificação (pendência #4) | Dívida de testes |
| **P2** | Substituir `ControleInatividadeMiddleware` (pendência #3) | Limpeza arquitetural |
| **P2** | E-mail: refatorar testes (pendência #9) | Dívida de testes |
| **P2** | E-mail: avaliar migração LLM cloud (pendência #13) | Só após 1 semana da Etapa 0 em prod + aval LGPD. Etapa 0 sozinha pode tornar a migração desnecessária. |
| **P3** | Refator CSS legado (pendências #5 e #6) | Limpeza sem ganho funcional |
| **P3** | E-mail: testar 7B (pendência #12) | Ganho marginal se 14B já satisfaz após parser regex |

**Caminho mínimo para produção:** completar P0. Restante pode entrar em releases incrementais.

---

## Módulo Relatórios — Backlog planejado (Mai/2026)

✅ **MVP de 5 relatórios IMPLEMENTADO em 2026-05-17** (ver entrada na tabela acima). Tela `/sankhya/relatorios/` ativa com Top Clientes/Produtos · Lotes Envelhecidos · Consumo por Veículo · Fluxo de Caixa · Margem por Venda. Backlog abaixo lista os **20 restantes** pra serem priorizados após feedback operacional do MVP.

### Eixo 1 — FINANCEIRO (alta prioridade pra gestor)

- **Fluxo de caixa projetado (30/60/90d)** — TGFFIN em aberto agrupado por semana/mês (entradas TOP 11+10, saídas TOP 53+13). Hoje não há consolidado.
- **Contas a pagar vencendo (próximos 7/15/30 dias)** — TGFFIN despesa com DHBAIXA=NULL, agrupado por fornecedor e por TOP. Útil pra negociar prazos.
- **Margem por venda faturada** — TOP 35/37 STATUSNOTA='L' com `VLRNOTA − (Σ TOP 11 do lote × VLRUNIT proporcional)`. Mostra cliente/produto com margem real.
- **Inadimplência** — TGFFIN receita vencida sem baixa, por parceiro. Lista cliente atrasando.

### Eixo 2 — VENDAS

- **Top clientes (período)** — por valor, kg, nº de pedidos. Filtro por TOP (34/35/37). Identifica perda de ritmo.
- **Top produtos vendidos** — descritivo + ranking de margem.
- **Curva ABC de cliente e de produto** — agrupa 80/20.
- **Pedidos não faturados envelhecidos** — TOP 34 STATUSNOTA<>'L' há > X dias. Cobra fechamento.
- **Avarias e devoluções (TOP 30 + TOP 36)** — % sobre vendas, por cliente, por produto, por motivo. Aponta qualidade-problema.

### Eixo 3 — COMPRAS / ESTOQUE (núcleo agro)

- **Saldo de lotes envelhecidos** — view `ANDRE_IAGRO_SALDO_LOTE` + `DTNEG_ORIGEM > 30/60d`. Pré-perda — vende ou abate preço.
- **Giro de lote por fornecedor** — tempo médio entre TOP 11 (entrada) e TOP 35/37 (venda) por NOMEPARC_ORIGEM.
- **Aproveitamento por lote** — `(qtd_vendida + qtd_classificada) / qtd_comprada`. Filtra fornecedor com muito descarte (`AD_QTDAVARIA` alta — perna E).
- **Compras por fornecedor (período)** — valor, kg, nº notas. Concentração de compras.
- **Rendimento da classificação** — % de cada lote: CLASSIFICADO (A) vs NAO_CLASSIFICAVEL (B) vs AVARIA (E). Por fornecedor.

### Eixo 4 — RASTREIO / WMS

- **Lotes sem vínculo a pedido** — saldo disponível mas sem TOP 34 alocado. Estoque parado.
- **Pedidos faturados sem lote** — TOP 34/35/37 STATUSNOTA='L' com CODAGREGACAO=NULL. Furo de rastreabilidade.
- **Notas órfãs (NOTA_ORFA)** — TOP 35/37 sem TGFVAR par e sem AD_VINCULO_PEDIDO_NOTA. Backlog de vinculação manual.

### Eixo 5 — COMBUSTÍVEL / FROTA

- **Consumo por veículo (km/L ou L/h)** — função `consultar_consumo_por_veiculo` já existe em `oracle_conn.py`. Falta tela amigável + comparativo entre veículos.
- **Custo de combustível por veículo / período** — Σ VLRTOT das requisições por CODVEICULO.
- **Abastecimento externo vs interno** — % do total gasto em postos terceiros (EXTERNA_POSTO). Indica oportunidade de centralizar.
- **Frequência de abastecimento por veículo** — identifica veículo "comendo muito".

### Eixo 6 — AUDITORIA / PRODUTIVIDADE

- **Lançamentos por operador (período)** — `AD_AUDITORIA_GERAL` agrupado por NOMEUSU + MODULO. Quem produz, quem está parado.
- **Operações canceladas/estornadas** — DELETE em AD_AUDITORIA_GERAL. Detecta retrabalho.
- **Pedidos importados por e-mail confirmados vs descartados** — `AD_PEDIDO_EMAIL_RECEBIDO` por STATUS. Mede produtividade LLM + operador.

### MVP recomendado (5 relatórios de maior impacto vs esforço)

| # | Relatório | Por que entrar primeiro |
|---|---|---|
| 1 | **Fluxo de caixa projetado (30/60/90d)** | Maior impacto decisório, dados já existem em TGFFIN, sem query nova complexa |
| 2 | **Top clientes + Top produtos (período)** | Base do "quem é meu negócio". Filtros simples sobre TGFCAB+TGFITE |
| 3 | **Lotes envelhecidos com saldo** | Evita perda direta de margem. View já calcula tudo |
| 4 | **Margem por venda (cliente × produto)** | Diferencia cliente bom de cliente ocupando capacidade. Requer JOIN entrada↔venda via CODAGREGACAO |
| 5 | **Consumo por veículo (com ranking)** | Frota entrou em produção esse mês — gestor quer ver |

**Esforço estimado MVP:** ~3-4 dias (todos são SELECT em queries existentes + tela única `/sankhya/relatorios/` com sub-abas + export Excel/PDF).

**Trade-off central:** quanto mais relatórios no MVP, mais demora o primeiro print em produção. Prefiro 5 sólidos com filtros bons e export funcional do que 15 superficiais.

**Decisões pendentes (perguntar antes de implementar):**
- Tela web vs export Excel/PDF programado por e-mail vs ambos
- Filtros padrão (período default, agrupamentos)
- Permissão (todos os grupos ou só Diretoria/Suporte como Auditoria)
- Cache de queries pesadas (margem por venda pode demorar)

---

## Decisões Técnicas Tomadas (preservar)

Decisões debatidas e aprovadas. **Não revisar sem discussão prévia.**

### Arquitetura e dados

- **Filtro cruzado do Rastreio** (Mai/2026 — 2026-05-25, simplificado): cross-filter vive nos campos únicos `q_lotes` (Lotes — bate em CODAGREGACAO/DESCRPROD/NOMEPARC_ORIGEM/NUNOTA_ORIGEM) e `q` / `cliente_q` (Pedidos — bate em NOMEPARC/NUNOTA/NUMNOTA). Backend dispara automaticamente. Click no header do pedido isola (`pedidoIsolado` + `codprodsIsolados`). _Histórico: checkboxes em dois eixos (`checksLotes`/`checksPorPedido`) existiram entre Abr e Mai/2026 e foram removidos por feedback operacional._
- **`CODNAT_POR_TOP`** centralizado em `oracle_conn.py`: `{34: 10010100, 35: 10010100, 37: 10010200}`.
- **Audit log do Rastreio em SQLite (`RastreioAudit`)**, não em tabela Oracle — separa telemetria da base de produção do ERP.
- **`humanizar_erro_oracle`** intercepta `ORA-XXXXX` e devolve frase amigável. Operador nunca vê stack trace. Mensagem técnica vai para `logger.exception` somente.
- **Lock pessimista (`SELECT ... FOR UPDATE`)** em `atribuir_lote_item_pedido` e `faturar_pedido_venda_banco` — defesa contra race conditions.
- **Atomicidade transacional explícita** em todas as views de escrita: `try/except → conn.rollback() → raise` dentro do `with obter_conexao_oracle()`.
- **Não emitir NFe automaticamente** após faturar — apenas marca `STATUSNOTA='L'`. Emissão real é responsabilidade do Sankhya. **Mantém domínio do ERP intocado.**
- **Lote no IAgro vive em `CODAGREGACAO`** (VARCHAR2), nunca em `CONTROLE`. Auto-gerado na Entrada (`NUNOTAS{SEQ}D{YYMMDD}`); livre na Venda; NULL em item de Venda até atribuição pelo Rastreio. Doc `.claude/schema.md` foi corrigida em Abr/2026 (entrada `CONTROLE` removida).
- **Pré-pedidos vindos de e-mail vivem em tabelas auxiliares Oracle (`AD_PEDIDO_EMAIL_RECEBIDO` + `AD_PEDIDO_EMAIL_ITEM`), não em SQLite nem em TGFCAB com TOP novo** (Mai/2026). Razão: backup unificado, joinable com TGFCAB via `NUNOTA_GERADO`, mas sem poluir queries existentes. **TGFCAB recebe INSERT só após confirmação humana**, reusando 100% das APIs da Venda.
- **Parser de PDF de pedido roda em LLM local (Ollama + Qwen 2.5 14B)**, nunca em API cloud (Mai/2026). Razão: dados de cliente não saem da máquina (LGPD/privacidade). Trade-off aceito: ~82-90% acurácia vs ~90-95% do cloud, com operador corrigindo na revisão. **Decisão revista no mesmo mês**: subido de 7B → 14B após confirmar que o servidor de produção (Xeon E5-2680 v4 + 64GB RAM) comporta com folga. Custo de tempo irrelevante no volume típico (5 PDFs/dia × ~80s = 7 min/dia). 32B disponível como opção experimental sem trocar default — `ollama pull qwen2.5:32b-instruct` e ajustar `OLLAMA_MODELO` no `.env` temporariamente.
- **Conta IMAP de pedidos:** `comercial@agromilagrocomercial.com.br` (Titan/Hostinger). **Pastas com hífen** (`Pedidos-Entrada`, `Pedidos-Processados`, `Pedidos-Erros`) — Titan trata como literal, sem nesting.
- **Split físico do PDF (e-mail Fase 1, Mai/2026)**: cada SUB_ID tem seu arquivo `<MSGID>_sub{N}.pdf` via `pypdf`. Operador vê só seu pedido no iframe. PDF original preservado. PDFs com 1 pedido só **NÃO criam** sub-arquivo. Comando `resplit_pdfs_email` faz backfill idempotente do legado.
- **Layouts plugáveis (e-mail Fase 2, Mai/2026)**: `_HEADERS_POR_LAYOUT` (worker) + `PROMPTS_POR_LAYOUT` (LLM). Adicionar layout = 2 entradas em dicts. Hoje: `CONSINCO_RELPED` (avisos específicos) + `GENERICO` (fallback neutro pra texto livre / fornecedor novo).
- **Paste manual (e-mail Fase 3, Mai/2026)**: botão `📋 Importar texto` na fila → registro com `PDF_PATH=NULL`, `ORIGEM='TEXTO_LIVRE'`. Frontend troca iframe por `<pre>` quando `pdf_path` ausente. Mesma fila e fluxo de revisão.
- **Vinculação por código do cliente (e-mail v4, Mai/2026)**: tabela `AD_CLIENTE_PRODUTO_COD` mapeia `(CODPARC, COD_CLIENTE) → CODPROD` direto. Etapa 0 do matching (antes de alias por descrição). Após operador confirmar 1× um cliente Consinco, **todos os pedidos seguintes daquele cliente casam itens automaticamente** com confiança 100%. Mais forte que alias por descrição porque é match exato em código numérico.
- **Schema-resilient via `_existe_coluna()`** (`oracle_conn.py`): cache 1× por processo Python detecta dinamicamente colunas opcionais (ORIGEM, COD_CLIENTE). Permite código rodar antes/depois das migrations sem ORA-00904. Reseta no restart do Django.
- **Defesa anti-duplicação em camadas (e-mail, Mai/2026)**: `api_email_reparser` faz batch DELETE pelo `RECEBIDO_ID` (atômico) + worker faz DELETE defensivo antes de cada INSERT. Já tivemos duplicação real quando a camada 1 falhou silenciosamente — defesa em camadas resolve.
- **TGFVAR é fonte canônica do vínculo pedido↔nota** (Rastreio Fase 2, Mai/2026 — 2026-05-11). Tabela **nativa Sankhya** populada via trigger interna. Diagnosticada em 2026-05-09 após confirmar que TGFCAB tem 6 campos NUNOTA-* mas todos NULL em produção (100% dos 298 pedidos TOP 34 dos últimos 30 dias) — o link real mora em `TGFVAR(NUNOTA, SEQUENCIA, NUNOTAORIG, SEQUENCIAORIG)`. Cobertura: 185k pares TOP 34↔35, 9k TOP 11↔13 (mesma mecânica do fluxo compra/vale que o user já conhecia). **IAgro só lê TGFVAR, nunca escreve** — trigger Sankhya mantém. Por isso a Fase 2 do Rastreio NÃO suporta split em pedido faturado: INSERT em TGFVAR exigiria entender o trigger.
- **`AD_NUMPEDIDOORIG` é convenção customizada da Agromil** (Mai/2026), não nativa Sankhya. Default populado automaticamente em `inserir_cabecalho_nota_banco` (linha 597: `int(dados.get('AD_NUMPEDIDOORIG') or novo_nunota)`). Funciona em pedidos criados pela IAgro (Vendas + Importação e-mail) e no fatura interno (UPDATE in-place do mesmo NUNOTA). **Mas em produção atual está NULL em 100% dos pedidos recentes** — operadores ainda criam direto no Sankhya. Migração retroativa via TGFVAR é pendência futura. Decisão: continuar populando pra preparar terreno; usar TGFVAR pro vínculo real.
- **Risco fiscal de alterar lote em nota faturada: zero** (validado em 2026-05-10 com XML real da Agromil — NFe 6242, CENOURA EXTRA NCM 0706). Grupo `<rastro>` da NFe 4.0 não está no XML — hortifrúti não exige rastreabilidade fiscal de lote. `CODAGREGACAO` mora apenas em TGFITE (interno). Premissa que destravou a Fase 2 do Rastreio.

### UX e visual

- **Paleta sóbria alinhada ao `home.css`:** `--ras-primary: #5e7e4a` (verde Agromil), `--ras-secondary: #825e38` (marrom), `--ras-accent: #38292c`. **Não usar gradientes saturados (azul→roxo).**
- **POR PARCEIRO ordenação:** data **ASC** + parceiro ASC (mostra "fila de pedidos a atender" cronologicamente).
- **POR PRODUTO ordenação:** data DESC + parceiro ASC.
- **Default colapsado:** todo grupo começa fechado. `pedidosJaVistos` / `gruposProdutoJaVistos` rastreiam quais já foram inicializados para preservar escolha do usuário em refresh/scroll.
- **Click na linha não marca o checkbox** — linha vira **selecionada** (revela botões 🔗/👁); checkbox tem listener próprio.
- **Botões de ação ocultos por padrão**, revelam-se com hover (opacity 0.55) ou seleção da linha (cresce de 26→32 com transition cubic-bezier "spring"). Reduz ruído visual em listas longas.
- **Bar "Lote armado"** no rodapé (`position: fixed; bottom: 36px`), não no topo. Visível durante scroll, fora do caminho de leitura.
- **Modais de confirmação custom** (`IAgro.confirmarAcao`) substituem `window.confirm()` em ações destrutivas.
- **Filtros da Venda vivem no card lateral `#cardFiltros`** (Abr/2026). Tentativa de mover para o header inline foi revertida por conflito visual: tirava espaço útil da tabela e poluía o cabeçalho. Padrão aprovado: form na lateral + chips de filtros ativos no rodapé do mesmo card + botões Atualizar/Limpar.

### Frontend

- **JS puro, sem framework.** Decisão histórica do projeto. Helpers em `iagro_helpers.js` cobrem o que precisaria de framework (cache, debounce, modal).
- **Avatar de fornecedor/produto com cor por hash do nome** — mesmo nome sempre mesma cor. Estável entre sessões e fácil de escanear visualmente.
- **`:has()` CSS** para estilização dependente de checkbox. Requer browser moderno (Chrome 105+, Safari 15.4+, Firefox 121+).
- **Persistência de preferências de filtro em `localStorage` com chave versionada** (Abr/2026). Venda usa `iagro:venda:filtros:v1`; Rastreio usa `iagro:rastreio:prefs:v1`. Padrão: salvar a cada `carregarVendas(false)` (ou equivalente), restaurar no boot, botão Limpar apaga o storage. Ao mudar formato de qualquer chave: **bumpar a versão** (`v2`) ou adicionar migração leve no carregar.
- **Typeahead de Lote no portal Venda foi descartado por agora** (Abr/2026). Refino apenas cosmético no input (placeholder "Código do lote" + title explicando `CODAGREGACAO` no formato `NUNOTAS{SEQ}D{YYMMDD}`). Razão: typeahead exigiria nova função em `oracle_conn.py`, bloqueada pela regra crítica #4.
