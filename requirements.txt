asgiref==3.9.1
cffi==2.0.0
charset-normalizer==3.4.3
cryptography==45.0.7
Django>=6.0,<7.0
oracledb>=3.4.2
pillow==11.3.0
pycparser==2.23
python-dotenv==1.1.1
reportlab==4.4.3
requests>=2.31.0
sqlparse==0.5.3
tzdata==2025.2
# Importação de pedidos por e-mail (IMAP + parser PDF + LLM local Ollama)
imap-tools>=1.6.0
pdfplumber>=0.11.0
ollama>=0.3.0
# Note: cx_Oracle is intentionally not installed on Windows/Python 3.13.
# The project uses python-oracledb (oracledb) as a fallback in
# sankhya_integration/services/oracle_conn.py, which is the modern,
# supported driver. If you must use the legacy cx_Oracle, install
# Microsoft C++ Build Tools and a version compatible with your Python,
# or pin Python to a version with prebuilt wheels.
